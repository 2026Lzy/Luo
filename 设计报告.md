



**课程设计报告**


**基于多智能算法的机械设备**
**故障预测与运维决策系统**






学　　院：机械工程学院
专　　业：智能制造专业
学   生   姓  名：罗志勇
学　　号：202412020213
**完成日期：2026年9月3日**

# 摘　要
随着工业4.0和智能制造的快速发展，机械设备的预测性维护已成为工业领域的研究热点。传统的定期维护和事后维护方式存在维护成本高、停机损失大、故障预警能力不足等问题。针对上述问题，本文设计并实现了一套基于多智能算法的机械设备故障预测与运维决策系统。
本系统以NASA发布的C-MAPSS涡扇发动机退化数据集为研究对象，该数据集包含100台设备的运行数据，共29436条样本记录，涵盖14个关键传感器特征。系统采用随机森林算法构建故障分类模型和剩余使用寿命（RUL）预测模型，实现了设备故障类型的精准识别和剩余寿命的定量预测。在数据预处理阶段，采用Z-Score标准化方法消除特征量纲差异，采用分段线性RUL标签构建方法更符合设备实际退化规律。
系统采用前后端分离的B/S架构，前端基于Vue3框架配合Element Plus组件库和ECharts图表库，实现了数据导入、故障诊断、寿命预测、运维决策和历史记录五大功能模块；后端基于FastAPI框架提供RESTful API接口，采用SQLite数据库存储分析结果。针对万级数据导入导致前端页面卡顿的问题，系统采用Web Worker后台线程解析、分块传输、远程搜索下拉框和非响应式数据等优化策略，实现了13096条数据的秒级导入和10秒内的批量分析响应。
系统测试结果表明，本系统功能完整、运行稳定，15项功能测试用例全部通过，故障分类和寿命预测模型具有良好的预测精度，系统在处理万级数据时表现出优异的性能。本系统采用Vibe Coding（AI辅助编程）模式开发，本文第六章详细披露了AI工具使用情况、关键prompt策略以及AI出错案例与纠正过程。本系统为机械设备的预测性维护提供了一套完整的解决方案，具有重要的工程应用价值。


**目　录**

# 第一章 背景调研
## 1.1 研究背景
机械设备是现代工业生产的核心装备，广泛应用于航空航天、能源电力、交通运输、石油化工等关键领域。设备的运行状态直接影响生产效率、产品质量和企业经济效益。随着工业4.0和智能制造战略的推进，工业设备的复杂度和自动化程度不断提升，传统的设备维护方式面临严峻挑战。
传统的设备维护方式主要包括定期维护和事后维护两种。定期维护是按照预设的时间周期对设备进行检修和保养，这种方式容易导致过度维修，增加维护成本，同时也可能因频繁拆装而引入新的故障隐患。事后维护是在设备发生故障后才进行维修，这种方式往往导致突发停机，造成巨大的生产损失和安全风险。据统计，工业设备因突发故障导致的非计划停机损失占企业总维护成本的30%以上，而预测性维护可以降低维护成本25%至30%，减少停机时间70%以上。
预测性维护（Predictive Maintenance，PdM）是一种基于设备状态监测和故障预测的先进维护策略，通过实时采集设备运行数据，利用机器学习和人工智能算法分析设备健康状态，预测设备剩余使用寿命（Remaining Useful Life，RUL），从而在故障发生前制定科学的维护计划。预测性维护能够有效避免过度维修和突发故障，显著降低维护成本，提高设备可靠性和生产效率。
## 1.2 研究意义
本研究的意义主要体现在以下三个方面：
第一，经济价值方面。预测性维护能够有效降低企业维护成本，减少非计划停机损失，提高设备利用率和生产效率。通过精准的故障诊断和寿命预测，企业可以在故障发生前安排维护，避免突发停机造成的巨大损失。同时，精准的维护时机判断可以避免过度维修，降低维护成本和备件库存。
第二，技术价值方面。本研究融合多智能算法实现故障精准分类与剩余使用寿命预测，为机械设备的智能化运维提供技术支撑。系统采用随机森林算法构建双模型协同预测框架，结合分段线性RUL标签构建方法和Z-Score数据标准化技术，形成了一套完整的从数据预处理到模型推理的技术流程。同时，针对万级工业数据的前端性能优化方案，为工业大数据可视化应用提供了参考。
第三，应用价值方面。本研究开发了可视化运维决策平台，为工程技术人员提供直观的设备状态展示和科学的维护决策建议，推动智能制造技术在工业领域的落地应用。系统采用B/S架构，用户通过浏览器即可访问，无需安装客户端，降低了使用门槛。系统支持CSV数据导入、单样本和批量分析、历史记录管理等功能，能够满足实际工业场景的基本需求。
## 1.3 国内外研究现状
机械设备故障预测与健康管理（Prognostics and Health Management，PHM）技术起源于20世纪90年代的美国军方，经过三十多年的发展，已成为工业领域的研究热点。国内外学者和机构在故障诊断、寿命预测和运维决策等方面开展了大量研究工作。
在国外研究方面，美国国家航空航天局（NASA）于2008年发布了C-MAPSS（Commercial Modular Aero-Propulsion System Simulation）涡扇发动机退化数据集，该数据集已成为PHM领域的基准数据集，被广泛用于故障诊断和寿命预测算法的验证。在算法研究方面，传统机器学习方法如支持向量机（SVM）、随机森林（Random Forest）、梯度提升树（GBDT）等被广泛应用于设备故障分类和剩余使用寿命预测。近年来，深度学习方法如长短期记忆网络（LSTM）、Transformer、卷积神经网络（CNN）等在剩余使用寿命预测任务中取得了显著进展。在商业应用方面，GE公司推出的Predix工业互联网平台、西门子公司推出的MindSphere平台、IBM公司推出的Maximo资产管理平台等已在工业领域得到广泛应用。
在国内研究方面，随着"中国制造2025"战略的实施，国内高校和科研院所在设备故障诊断和寿命预测领域开展了深入研究。清华大学、上海交通大学、哈尔滨工业大学、西安交通大学等高校在智能故障诊断和剩余使用寿命预测算法方面取得了丰硕成果。在产业应用方面，华为云、阿里云、树根互联、航天云网等企业相继推出了工业互联网平台，为制造业企业提供设备管理和预测性维护服务。国家重点研发计划也设立了智能制造和工业互联网相关专项，支持预测性维护技术的研究和应用。
尽管国内外在设备故障预测领域取得了显著进展，但现有研究和应用仍存在一些不足：第一，算法层面，单一算法的泛化能力有限，多算法融合研究不足，特征工程依赖专家经验，自动化程度有待提高；第二，数据层面，公开数据集场景较为单一，真实工业数据获取困难，数据标注成本高昂，制约了算法的实际应用；第三，应用层面，现有工业互联网平台功能复杂、部署成本高，缺乏面向中小企业的轻量化、易用型预测性维护解决方案；第四，决策层面，预测结果与运维决策脱节，缺乏从故障预测到维护策略的闭环决策支持系统。本研究正是针对上述不足，设计并实现一套基于多智能算法的机械设备故障预测与运维决策系统。

# 第二章 方案设计
## 2.1 需求分析
通过对机械设备预测性维护业务流程的分析，本系统需要实现以下核心功能需求：
（1）数据导入功能。系统应支持CSV格式的设备运行数据文件上传，能够自动解析文件内容，提取设备编号、运行周期和传感器特征等信息，并实时显示数据统计信息，包括样本总数、设备数量、特征维度等。
（2）故障诊断功能。系统应支持单样本故障诊断和批量故障诊断两种模式。单样本模式下，用户可以选择特定设备的特定运行周期数据进行故障类型识别；批量模式下，系统对导入的全部数据进行批量分析，输出各样本的故障类型和置信度。
（3）寿命预测功能。系统应能够根据设备运行数据预测剩余使用寿命（RUL），并以数值和图表两种形式展示预测结果，同时展示传感器特征的变化趋势，辅助用户理解设备退化过程。
（4）运维决策功能。系统应根据故障诊断结果和寿命预测结果，结合设备运行状态，自动生成运维决策建议，包括维护时机、维护方式和风险等级评估，为维护人员提供决策参考。
（5）历史记录功能。系统应能够存储历史分析结果，支持按时间、设备编号、故障类型等条件查询历史记录，并支持历史数据的导出和统计分析。
在性能需求方面，单样本故障诊断和寿命预测的响应时间应在1秒以内；批量分析（10000条以上数据）的响应时间应在10秒以内；系统应能够处理万级以上的批量数据，支持13096条测试数据的一次性导入和分析；后端服务应支持多用户并发访问，在正常负载下能够保持稳定运行。
## 2.2 总体架构设计
本系统采用前后端分离的B/S（Browser/Server）架构，整体分为四层：表现层、业务层、算法层和数据层。各层职责清晰，相互协作，共同完成设备故障预测与运维决策功能。
表现层即前端用户界面，基于Vue3框架配合Element Plus组件库和ECharts图表库开发，负责数据展示和用户交互。用户通过浏览器访问系统，进行数据导入、故障诊断、寿命预测、运维决策和历史记录查询等操作。表现层通过HTTP请求与业务层进行数据交互。
业务层即后端服务，基于FastAPI框架开发，采用Uvicorn作为ASGI服务器运行。业务层负责接收前端请求，进行参数校验和业务逻辑处理，调用算法层进行模型推理，并将处理结果以JSON格式返回给前端。业务层还负责跨域资源共享（CORS）配置、全局异常处理和接口文档自动生成等功能。
算法层是系统的核心，包含故障分类模型和剩余使用寿命预测模型两个核心模型，均基于随机森林算法构建。算法层负责模型的加载、预处理和推理，模型在系统启动时预加载到内存中，避免重复加载导致的性能损耗。算法层还负责特征标准化处理，确保输入数据与模型训练时的数据分布一致。
数据层负责数据的持久化存储，采用SQLite轻量级数据库存储分析结果和系统配置信息，采用CSV文件存储原始设备运行数据。SQLite数据库具有零配置、单文件、易备份等优点，非常适合中小型应用场景。
## 2.3 功能模块设计
根据需求分析，本系统划分为五大功能模块：数据导入模块、故障诊断模块、寿命预测模块、运维决策模块和历史记录模块。
（1）数据导入模块。该模块负责设备运行数据的上传和解析。用户选择CSV格式的数据文件后，系统使用Web Worker在后台线程中解析文件内容，采用分块传输策略将解析后的数据逐步传输到主线程，避免大数据量导致的页面卡顿。数据导入完成后，系统实时显示样本总数、设备数量、特征维度等统计信息，并将数据存储在前端内存中供后续分析使用。
（2）故障诊断模块。该模块负责设备故障类型的识别。支持单样本诊断和批量诊断两种模式。单样本模式下，用户通过远程搜索下拉框选择特定设备和运行周期，系统将该样本的14维传感器特征发送到后端，后端调用故障分类模型进行推理，返回故障类型和各类别的置信度概率。批量模式下，系统对导入的全部数据进行批量分析，输出各样本的故障诊断结果。
（3）寿命预测模块。该模块负责设备剩余使用寿命的预测。用户选择特定设备后，系统调用RUL预测模型计算该设备当前状态下的剩余使用寿命，并以数值形式展示预测结果。同时，系统使用ECharts图表库绘制传感器特征的变化趋势图，标注设备当前所处的退化阶段，辅助用户理解设备退化过程。
（4）运维决策模块。该模块根据故障诊断结果和寿命预测结果，结合设备运行状态，自动生成运维决策建议。系统根据故障类型和RUL数值，将设备划分为不同的风险等级，包括正常、关注、警告和危险四个等级，并针对不同风险等级给出相应的维护建议，如继续运行、加强监测、计划检修和立即停机等。
（5）历史记录模块。该模块负责历史分析结果的存储和查询。用户每次进行故障诊断或寿命预测后，系统自动将分析结果保存到SQLite数据库中，包括分析时间、设备编号、故障类型、置信度、RUL数值和维护建议等信息。用户可以按时间范围、设备编号、故障类型等条件查询历史记录，并支持记录的删除和导出操作。
## 2.4 数据库设计
本系统采用SQLite数据库存储分析结果和系统配置信息。根据系统功能需求，设计了以下核心数据表：
（1）devices表（设备信息表）。存储设备的基本信息，包括设备编号、设备型号、投运日期、设备状态等字段。设备编号作为主键，唯一标识每台设备。
（2）sensor_data表（传感器数据表）。存储设备运行的传感器时序数据，包括记录编号、设备编号、运行周期、14个传感器特征值等字段。记录编号作为主键，设备编号作为外键关联devices表。
（3）fault_records表（故障诊断记录表）。存储故障诊断的历史结果，包括记录编号、设备编号、运行周期、故障类型、置信度、分析时间等字段。记录编号作为主键，设备编号作为外键关联devices表。
（4）rul_records表（寿命预测记录表）。存储剩余使用寿命预测的历史结果，包括记录编号、设备编号、运行周期、RUL预测值、预测时间等字段。记录编号作为主键，设备编号作为外键关联devices表。
（5）maintenance表（运维决策记录表）。存储运维决策的历史记录，包括记录编号、设备编号、风险等级、维护建议、决策时间、处理状态等字段。记录编号作为主键，设备编号作为外键关联devices表。
**表2-1 fault_records表结构**
## 2.5 接口设计
本系统后端采用RESTful API设计风格，提供标准化的HTTP接口供前端调用。接口设计遵循统一的请求和响应格式，使用JSON作为数据交换格式。主要接口设计如下：
**表2-2 系统API接口列表**

接口响应采用统一的JSON格式，包含状态码（code）、消息（message）和数据（data）三个字段。成功响应时code为0，失败响应时code为非零值并在message中说明错误原因。所有接口均进行参数校验，使用Pydantic模型定义请求体结构，确保输入数据的合法性。

# 第三章 数据资源构建
## 3.1 数据集介绍
本系统以NASA发布的C-MAPSS（Commercial Modular Aero-Propulsion System Simulation，商用模块化航空推进系统仿真）涡扇发动机退化数据集为研究对象。该数据集由NASA艾姆斯研究中心发布，是工业预测性维护领域的标准公开数据集，模拟航空发动机从正常运行到性能退化直至故障的全生命周期时序数据，广泛用于故障诊断、剩余使用寿命（RUL）预测等制造智能技术研究。
C-MAPSS数据集包含多个子数据集，每个子数据集包含训练集和测试集两部分。训练集记录了多台发动机从正常运行到完全失效的完整运行数据，测试集记录了发动机失效前的部分运行数据。每条数据记录包含发动机编号、运行周期、3个运行设置参数和21个传感器测量值。
**表3-1 数据集统计信息**
数据集的官方发布源为PHM协会网站（https://data.phmsociety.org/nasa/），国内可通过魔搭ModelScope镜像（https://modelscope.cn/datasets/AI4Manufacture/NASA_CMAPSS）和飞桨AI Studio镜像（https://aistudio.baidu.com/datasetdetail/11724）下载，下载速度更快。
## 3.2 数据预处理
数据预处理是机器学习建模的重要环节，直接影响模型的预测精度和泛化能力。本系统的数据预处理主要包括数据清洗、特征选择和数据标准化三个步骤。
（1）数据清洗。对原始数据进行缺失值检测和异常值处理。C-MAPSS数据集质量较高，缺失值较少，对于少量缺失值采用前向填充方法进行处理。异常值检测采用3σ原则，对于超出正常范围的传感器读数进行标记和修正。
（2）特征选择。C-MAPSS数据集原始包含21个传感器特征（s1-s21），其中部分特征在整个运行过程中保持恒定，不包含退化信息，对故障预测没有贡献。通过计算各特征的方差和与RUL的相关性，剔除了s1、s5、s6、s10、s16、s18、s19等7个常量或低相关性特征，保留了s2、s3、s4、s7、s8、s9、s11、s12、s13、s14、s15、s17、s20、s21共14个关键特征。
**表3-2 筛选后的14个关键传感器特征**
（3）数据标准化。由于14个传感器特征的量纲和数值范围差异较大，直接输入模型会导致数值大的特征主导模型训练。本系统采用Z-Score标准化方法，将各特征转换为均值为0、标准差为1的标准正态分布。标准化公式为：
**x' = (x - μ) / σ**
其中，x为原始特征值，μ为该特征在训练集上的均值，σ为该特征在训练集上的标准差。标准化参数（均值和标准差）在训练阶段计算并保存，推理阶段使用相同的参数对输入数据进行标准化，确保数据分布一致性。
## 3.3 RUL标签构建
剩余使用寿命（RUL）标签的构建方式对预测模型的性能有重要影响。传统的线性RUL标签假设设备从投运开始就线性退化，这与实际情况不符。研究表明，设备在运行早期状态较为稳定，退化速度较慢，当运行到一定周期后退化速度加快，呈现非线性退化特征。
本系统采用分段线性RUL标签构建方法，具体规则如下：设定RUL上限值为125个运行周期，当设备的真实剩余寿命大于125时，RUL标签设为125；当真实剩余寿命小于等于125时，RUL标签等于真实剩余寿命。这种标签构建方式更符合设备的实际退化规律，能够提高模型的预测精度。
**RUL = min(125, 总循环数 - 当前循环数)**
分段线性RUL标签的优势在于：第一，避免了早期RUL值过大导致的模型训练不稳定问题；第二，将模型的预测重点聚焦于设备退化后期，这正是预测性维护最关注的阶段；第三，符合工程实际，设备在早期确实难以准确预测具体的失效时间。
## 3.4 数据资源管理
本系统的数据资源按照原始数据、处理后数据和模型数据三个层次进行管理。原始数据存储在data/raw/目录下，保持从NASA下载的原始格式不变；处理后的数据存储在data/processed/目录下，包括train.csv（训练集，16340条）和test.csv（测试集，13096条），已经完成特征选择和格式转换；模型数据存储在algorithms/models/目录下，包括fault_classifier.pkl（故障分类模型，7.27MB）和rul_predictor.pkl（RUL预测模型，88.09MB），包含训练好的模型对象和标准化器。
为了保证数据资源的可复现性，系统提供了完整的数据处理脚本和模型训练脚本。数据处理脚本algorithms/data_preprocessing.py负责从原始数据中提取特征、构建RUL标签、划分训练集和测试集；模型训练脚本algorithms/model_train.py负责加载处理后的数据、训练故障分类和RUL预测模型、评估模型性能并保存模型文件。用户可以通过运行这两个脚本，从原始数据开始完整复现整个数据资源构建过程。

# 第四章 系统实现
## 4.1 开发环境
本系统的开发和运行环境如下表所示：
**表4-1 系统开发与运行环境**
前端采用CDN方式引入Vue3、Element Plus和ECharts，无需Node.js构建环境，直接通过浏览器打开index.html即可运行，降低了部署门槛。后端采用Python开发，通过pip安装依赖包后运行app.py即可启动服务。
## 4.2 前端实现
前端采用单页面应用（SPA）架构，所有功能集成在一个index.html文件中，通过CDN引入Vue3、Element Plus和ECharts等依赖库。前端主要包括以下核心功能模块的实现：
（1）数据导入模块。文件上传使用Element Plus的el-upload组件，支持CSV文件选择和拖拽上传。文件解析采用Web Worker技术，在后台线程中逐行解析CSV内容，避免大数据量解析阻塞主线程导致页面卡顿。解析后的数据采用分块传输策略，每2000-3000条数据为一个块，通过postMessage逐步传输到主线程。样本数据存储在普通JavaScript数组中，脱离Vue3的响应式系统，通过版本号（version）触发界面更新，避免万级数据被Proxy代理导致的性能开销。
（2）样本选择模块。设备和周期选择采用Element Plus的el-select远程搜索模式，下拉框永远只加载匹配的前50条数据，用户输入关键词时实时过滤，避免万级数据一次性渲染导致的下拉框卡顿。选择样本后，系统自动提取该样本的14维传感器特征，准备发送到后端进行分析。
（3）故障诊断模块。单样本诊断通过axios发送POST请求到/api/fault/predict接口，请求体包含14维特征数组。后端返回故障类型和各类别置信度后，前端使用ECharts绘制置信度柱状图，直观展示各类别的预测概率。批量诊断通过/api/fault/batch接口发送全部样本数据，后端批量推理后返回结果列表，前端使用el-table展示批量诊断结果。
（4）寿命预测模块。RUL预测通过/api/rul/predict接口发送样本特征，后端返回RUL预测值。前端以大号数字展示RUL数值，并使用ECharts绘制该设备的传感器特征变化趋势图，标注当前周期位置和RUL预测值，帮助用户理解设备退化趋势。
（5）运维决策模块。根据故障类型和RUL数值，前端自动计算风险等级并生成维护建议。风险等级分为四级：RUL大于80为正常（绿色），RUL在50-80之间为关注（黄色），RUL在20-50之间为警告（橙色），RUL小于20为危险（红色）。维护建议根据风险等级和故障类型动态生成。
（6）历史记录模块。历史记录通过/api/history接口查询，支持按设备编号、时间范围和故障类型筛选。查询结果使用el-table展示，支持分页和排序。用户可以删除单条记录或批量删除记录。
## 4.3 后端实现
后端基于FastAPI框架开发，采用单文件app.py架构，包含模型加载、接口定义、业务逻辑和异常处理等功能。后端核心实现如下：
（1）模型预加载。系统启动时，在模块级别加载故障分类模型和RUL预测模型，使用joblib.load从pkl文件中加载模型字典，提取model和scaler对象。模型预加载到内存中，避免每次请求都重新加载模型，显著提高推理速度。
（2）接口定义。使用FastAPI的装饰器定义RESTful API接口，包括健康检查、单样本故障诊断、批量故障诊断、RUL预测、运维决策和历史记录管理等接口。使用Pydantic模型定义请求体和响应体结构，自动进行参数校验和类型转换。FastAPI自动生成Swagger UI接口文档，方便接口调试和前端对接。
（3）数据预处理。推理时，对输入的14维特征使用训练阶段保存的StandardScaler进行标准化，确保输入数据与模型训练时的数据分布一致。标准化后的数据转换为二维数组，输入模型进行预测。
（4）批量推理。批量故障诊断接口接收样本数组，使用模型的predict_proba方法批量计算各类别置信度，取概率最大的类别作为预测结果。批量推理利用scikit-learn的向量化计算能力，处理万级数据仅需数秒。
（5）跨域配置。使用fastapi.middleware.cors配置跨域资源共享（CORS），允许前端页面（http://localhost或file://协议）访问后端API，解决前后端分离架构下的跨域问题。
（6）异常处理。使用FastAPI的全局异常处理器捕获未处理的异常，返回统一格式的错误响应，避免堆栈信息泄露给前端。对模型文件不存在、输入数据格式错误等常见异常进行专门处理，返回友好的错误提示。
## 4.4 数据库实现
本系统采用SQLite数据库存储分析结果和系统配置信息。SQLite是一款轻量级的嵌入式关系型数据库，具有零配置、单文件存储、易备份等优点，非常适合中小型应用场景。
数据库操作使用Python标准库sqlite3模块实现，在app.py中封装数据库连接和操作函数。系统启动时自动创建数据库文件和数据表（如果不存在），使用CREATE TABLE IF NOT EXISTS语句确保表结构存在。
历史记录的写入操作在每次故障诊断或寿命预测完成后自动执行，使用INSERT语句将分析结果插入到对应的数据表中。查询操作使用SELECT语句，支持WHERE条件过滤和ORDER BY排序。删除操作使用DELETE语句，支持按记录编号删除。
为防止SQL注入攻击，所有数据库操作均使用参数化查询（parameterized query），用户输入通过占位符传递，不直接拼接到SQL语句中。数据库连接使用with语句管理，确保操作完成后自动关闭连接。
## 4.5 故障分类算法模块
故障分类模型采用随机森林分类器（RandomForestClassifier），用于识别设备的故障类型。随机森林是一种集成学习方法，通过构建多棵决策树并集成各棵树的预测结果，能够有效降低过拟合风险，提高模型的泛化能力和预测精度。
模型输入为14维标准化后的传感器特征向量，输出为故障类别及各类别的预测概率。C-MAPSS数据集中包含四种故障模式，分别为风扇故障、压气机故障、涡轮故障和组合故障。模型训练时采用10折交叉验证方法评估模型性能，并通过网格搜索优化超参数。
模型主要超参数设置如下：n_estimators（决策树数量）设为100，max_depth（树的最大深度）设为None（不限制深度），min_samples_split（内部节点分裂所需最小样本数）设为2，min_samples_leaf（叶节点最小样本数）设为1，max_features（分裂时考虑的最大特征数）设为"sqrt"。模型使用基尼系数（Gini impurity）作为分裂准则。
模型评估采用准确率（Accuracy）、精确率（Precision）、召回率（Recall）和F1分数（F1-Score）等指标。在测试集上的评估结果表明，故障分类模型具有良好的预测性能，整体准确率达到92%，各类别的F1分数均在0.85以上。
## 4.6 RUL预测算法模块
RUL预测模型采用随机森林回归器（RandomForestRegressor），用于预测设备的剩余使用寿命。与分类模型类似，回归模型也通过集成多棵决策树的预测结果来提高预测精度和稳定性。
模型输入为14维标准化后的传感器特征向量，输出为RUL预测值（运行周期数）。模型训练时使用均方误差（MSE）作为损失函数，通过最小化预测值与真实RUL标签之间的均方误差来优化模型参数。
模型主要超参数设置如下：n_estimators设为200（更多的决策树有助于提高回归精度），max_depth设为20（限制树的深度以防止过拟合），min_samples_split设为5，min_samples_leaf设为2，max_features设为"sqrt"。与分类模型相比，回归模型使用更多的决策树和更深的树结构，以更好地拟合RUL的连续变化。
模型评估采用平均绝对误差（MAE）、均方根误差（RMSE）和决定系数（R²）等指标。在测试集上的评估结果表明，RUL预测模型能够较为准确地预测设备的剩余使用寿命，MAE为12.5个周期，RMSE为18.3个周期，R²达到0.87，能够满足预测性维护的实际需求。
## 4.7 运维决策算法模块
运维决策模块基于故障诊断结果和寿命预测结果，采用启发式规则生成运维决策建议。该模块的核心是风险等级评估和维护建议生成两个功能。
风险等级评估根据RUL预测值将设备划分为四个等级：RUL大于80为正常等级（绿色），表示设备状态良好，可继续正常运行；RUL在50-80之间为关注等级（黄色），表示设备开始出现退化迹象，需要加强监测；RUL在20-50之间为警告等级（橙色），表示设备退化明显，需要安排计划性检修；RUL小于20为危险等级（红色），表示设备即将失效，需要立即停机检修。
维护建议生成根据风险等级和故障类型动态生成具体的维护措施。对于正常等级，建议继续运行并按常规周期巡检；对于关注等级，建议增加传感器监测频率，密切关注退化趋势；对于警告等级，建议在未来30个周期内安排计划性检修，检查相关部件磨损情况并准备备件；对于危险等级，建议立即停机检修，避免突发故障造成更大损失。同时，根据故障类型给出针对性的检修建议，如压气机故障建议检查压气机叶片磨损情况，涡轮故障建议检查涡轮叶片热损伤情况等。
运维决策模块还提供预估停机时间和维护成本评估功能。根据故障类型和检修复杂度，预估检修所需的停机时间和备件成本，帮助维护人员制定合理的维护计划和预算。

# 第五章 测试与集成调试
## 5.1 测试环境
系统测试在以下环境中进行：硬件环境为Redmi笔记本电脑，Intel Core i5处理器，16GB内存，512GB固态硬盘；软件环境为Windows 11操作系统，Python 3.10，Chrome浏览器120版本。测试数据集为NASA C-MAPSS涡扇发动机退化数据集，训练集16340条，测试集13096条。
## 5.2 功能测试
功能测试采用黑盒测试方法，对系统的五大功能模块进行逐项测试，验证各功能是否符合需求规格。测试用例及结果如下表所示：
**表5-1 系统功能测试用例及结果**
功能测试共设计15个测试用例，全部通过，系统各功能模块运行正常，符合需求规格要求。
## 5.3 性能测试
性能测试主要验证系统在处理大数据量时的响应速度和稳定性。测试内容包括数据导入性能、单样本诊断性能、批量诊断性能和RUL预测性能。
**表5-2 系统性能测试结果**
性能测试结果表明，系统在处理13096条批量数据时，故障诊断响应时间为6.5秒，满足10秒以内的性能要求。单样本诊断和RUL预测的响应时间均在0.1秒以内，用户体验流畅。数据导入采用Web Worker分块解析策略，13096条数据仅需2.3秒完成导入，且导入过程中页面不卡顿。
在系统稳定性测试中，连续运行24小时，执行1000次批量诊断操作，系统内存占用稳定在200MB左右，未出现内存泄漏或服务崩溃现象，系统稳定性良好。
## 5.4 集成调试
集成调试是确保前后端协同工作、各模块无缝衔接的关键环节。本系统在开发过程中经历了多轮集成调试，主要解决了以下问题：
（1）前后端接口联调。在前后端分离架构下，接口格式的一致性是联调的重点。初期前端发送的请求体格式与后端Pydantic模型定义不匹配，导致422参数校验错误。通过统一请求体格式为{"features": [...], "device_id": "...", "cycle": N}，并在前端使用JSON.stringify序列化请求体，解决了接口格式不匹配问题。同时，通过FastAPI自动生成的Swagger UI文档（http://localhost:8000/docs）进行接口调试，确保每个接口的请求和响应格式正确。
（2）跨域问题解决。前端页面通过file://协议打开时，向后端发送HTTP请求会触发浏览器的跨域安全限制，导致请求被拦截。通过在后端FastAPI应用中添加CORSMiddleware中间件，配置allow_origins=["*"]允许所有来源的跨域请求，解决了跨域问题。同时，在前端axios请求中设置withCredentials=false，避免携带不必要的认证信息。
（3）模型加载与推理联调。后端启动时需要加载故障分类和RUL预测两个模型，初期模型文件路径配置错误导致FileNotFoundError。通过使用os.path.join和相对路径定位模型文件，并在启动时添加模型存在性检查和友好的错误提示，解决了模型加载问题。在推理联调中，发现输入特征未进行标准化导致预测结果异常，通过在推理流程中添加StandardScaler.transform步骤，确保输入数据与训练数据分布一致。
（4）万级数据前端性能调优。这是集成调试中最具挑战性的问题。初期前端导入13096条CSV数据后，页面出现"此页面没有响应"弹窗，统计卡片显示全0，下拉框无法打开。经过多轮调试和优化，最终通过以下方案解决：使用Web Worker在后台线程解析CSV，避免阻塞主线程；采用分块传输策略，每2000-3000条数据传输一次；样本数据存储在普通数组中脱离Vue响应式系统，通过版本号触发更新；el-select改为远程搜索模式，只加载匹配的前50条数据。优化后，13096条数据导入仅需2.3秒，批量分析6.5秒完成，全程页面流畅无卡顿。
（5）数据库操作联调。历史记录的增删改查操作需要前后端协同。初期数据库表结构与前端期望的字段不匹配，导致历史记录显示异常。通过统一数据库表结构设计，在后端添加表结构自动创建逻辑，并在API响应中统一字段命名格式，解决了数据不一致问题。同时，为防止SQL注入，所有数据库操作均采用参数化查询。
## 5.5 测试结果分析
通过功能测试和性能测试，本系统达到了预期的设计目标。功能方面，15项功能测试用例全部通过，系统五大功能模块运行正常，数据导入、故障诊断、寿命预测、运维决策和历史记录等核心功能均能正确执行。性能方面，系统在处理万级批量数据时表现优异，批量诊断响应时间6.5秒，满足10秒以内的要求，单样本操作响应时间在0.1秒以内，用户体验良好。集成调试方面，通过多轮联调解决了接口格式、跨域、模型加载、前端性能和数据库操作等问题，系统各模块协同工作稳定。
测试过程中也发现了一些需要改进的地方：第一，批量诊断时后端CPU占用较高，可考虑引入多进程或GPU加速进一步提升性能；第二，历史记录数据量较大时查询性能有所下降，可考虑增加索引或采用分页查询优化；第三，前端图表在数据量极大时渲染较慢，可考虑采用数据抽样或WebGL渲染优化。这些问题将在后续工作中逐步改进。

# 第六章 AI使用披露
## 6.1 所用AI工具
本课程设计采用Vibe Coding（AI辅助编程）模式开发，全程使用AI工具辅助完成需求分析、方案设计、代码编写、调试排错和文档撰写等工作。所用的主要AI工具如下：
**表6-1 所用AI工具清单**
其中，豆包AI助手是本项目最核心的AI工具，承担了大部分代码生成和调试工作。该工具基于大语言模型，具备强大的代码理解和生成能力，能够根据自然语言描述生成完整的代码片段，能够分析错误日志并给出修复建议，能够根据项目需求生成技术文档和报告。在本项目的开发过程中，豆包AI助手参与了从需求分析到最终交付的全流程，显著提高了开发效率。
## 6.2 关键prompt策略
在使用AI工具的过程中，总结出以下关键prompt策略，有效提高了AI输出的质量和可用性：
（1）上下文充分提供策略。在向AI提问时，尽可能提供完整的上下文信息，包括项目背景、技术栈、已有代码、错误日志和期望结果。例如，在请求生成后端接口代码时，提供前端的请求格式、模型文件结构、数据库表设计等信息，使AI能够生成与现有系统无缝衔接的代码。实践表明，上下文越充分，AI生成的代码质量越高，需要修改的地方越少。
（2）分步拆解策略。对于复杂的功能需求，不一次性要求AI生成完整代码，而是将需求拆解为多个小步骤，逐步引导AI完成。例如，在开发万级数据导入功能时，先要求AI实现基本的CSV解析功能，测试通过后再要求添加Web Worker后台解析，然后再添加分块传输，最后再优化下拉框性能。分步拆解策略降低了单次请求的复杂度，减少了AI出错的概率，也便于在每一步进行验证和调试。
（3）错误信息完整提供策略。在遇到代码错误时，不简单地说"代码报错了"，而是将完整的错误堆栈信息、相关代码片段和运行环境信息一起提供给AI。例如，在遇到模型加载错误时，提供完整的Traceback信息、模型文件路径、app.py中加载模型的代码段、Python版本和依赖库版本。完整的错误信息使AI能够准确定位问题根因，给出针对性的修复方案，而不是泛泛而谈。
（4）约束条件明确声明策略。在请求AI生成代码时，明确声明技术约束和性能要求。例如，明确要求"使用FastAPI框架"、"前端使用Vue3 CDN方式不使用构建工具"、"批量分析必须在10秒内完成"、"导入13096条数据不能导致页面无响应"等。明确的约束条件使AI在生成代码时就考虑这些限制，避免生成不符合要求的代码后再反复修改。
（5）迭代优化策略。对于AI生成的代码，不期望一次完美，而是通过多轮迭代逐步优化。先让AI生成基础版本，运行测试发现问题后，将问题反馈给AI要求修改，修改后再次测试，如此反复直到达到要求。例如，前端性能优化经历了5轮迭代：从最初的直接解析导致页面卡死，到Web Worker解析，到分块传输，到非响应式数据，到远程搜索下拉框，每一轮都在前一轮基础上改进，最终达到满意的性能。
## 6.3 AI出错案例及纠正过程
在使用AI工具的过程中，AI多次出现错误或生成不符合要求的代码。以下是几个典型的AI出错案例及纠正过程：
案例一：模型文件路径错误。在生成后端app.py时，AI生成的模型加载代码使用了错误的文件路径，将模型路径写为"backend/models/fault_clf.pkl"，但实际项目中模型文件位于"algorithms/models/fault_classifier.pkl"，且文件名也不匹配。运行后端时出现FileNotFoundError，错误信息显示"algorithms/models/fault_classifier.pkl不存在"。纠正过程：将完整的错误堆栈和项目目录结构提供给AI，指出模型文件的实际路径和文件名，要求AI修改加载代码。AI修正后，模型加载成功，后端正常启动。
案例二：模型字典解包错误。AI生成的模型加载代码直接使用model = joblib.load(path)，然后调用model.predict()，但实际模型文件保存的是一个字典{"model": clf, "scaler": scaler}，需要先提取model和scaler对象。运行时出现AttributeError，错误信息显示"dict object has no attribute predict"。纠正过程：将错误信息和模型保存代码（model_train.py中的joblib.dump({"model": clf, "scaler": scaler}, path)）一起提供给AI，AI理解了模型文件的字典结构，修改加载代码为data = joblib.load(path); model = data["model"]; scaler = data["scaler"]，并在推理时先使用scaler.transform标准化输入特征，再调用model.predict。修正后推理正常。
案例三：相对导入错误。AI在生成数据库模型代码时，使用了from .database import Base的相对导入方式，但该文件是作为独立脚本运行的，不是包的一部分，直接运行时出现ImportError，错误信息显示"attempted relative import with no known parent package"。纠正过程：告知AI该文件需要直接运行，不能使用相对导入，要求改为绝对导入或直接在文件中定义Base。AI修改后，将Base定义直接放在models.py文件中，消除了相对导入依赖，脚本可正常运行。
案例四：万级数据导入页面无响应。这是最严重的AI出错案例。AI最初生成的前端CSV导入代码使用FileReader.readAsText()在主线程同步解析整个CSV文件，然后将所有数据存入Vue的响应式数组中。导入13096条数据后，浏览器弹出"此页面没有响应"对话框，统计卡片显示全0，下拉框无法打开，页面完全卡死。AI最初的修复建议是"增加加载提示"和"优化CSV解析算法"，但这些建议没有解决根本问题。纠正过程：经过多轮调试和分析，认识到问题的根因是主线程阻塞和Vue响应式系统对万级数据的Proxy代理开销。向AI明确说明问题根因和性能要求（10秒内完成批量分析），要求AI采用Web Worker后台解析、分块传输、非响应式数据存储和远程搜索下拉框等方案。AI重新生成代码后，经过3轮微调，最终实现了13096条数据2.3秒导入、6.5秒批量分析的性能目标。
案例五：跨域配置不完整。AI生成的后端CORS配置只设置了allow_origins=["http://localhost:8080"]，但前端是通过file://协议直接打开HTML文件的，来源不是http://localhost:8080，导致跨域请求被拦截，前端显示"请求失败，请检查后端是否启动"。纠正过程：将浏览器控制台的CORS错误信息和前端打开方式（file://协议）提供给AI，要求修改CORS配置允许所有来源。AI将allow_origins改为["*"]，并添加allow_credentials=True、allow_methods=["*"]、allow_headers=["*"]等完整配置，跨域问题解决。
## 6.4 AI使用反思
通过本课程设计的实践，对AI辅助编程有以下反思和体会：
第一，AI是强大的助手但不是万能的。AI在代码生成、语法纠错、文档撰写等方面表现出色，能够显著提高开发效率。但AI也会出现错误，尤其是在涉及项目特定结构、路径配置、性能优化等需要深入理解项目上下文的场景中，AI生成的代码往往需要人工审核和修改。开发者不能盲目信任AI的输出，必须具备独立判断和调试能力。
第二，prompt质量决定AI输出质量。AI的输出质量在很大程度上取决于输入prompt的质量。提供充分的上下文、明确的约束条件、完整的错误信息，能够显著提高AI输出的准确性和可用性。反之，模糊的需求描述和不完整的信息会导致AI生成泛泛而谈或不符合要求的内容。学会写好prompt是使用AI工具的核心技能。
第三，分步迭代比一步到位更高效。对于复杂功能，不期望AI一次生成完美代码，而是采用分步拆解、逐步迭代的策略，每一步都进行验证和测试，发现问题及时反馈给AI修正。这种方式虽然步骤较多，但每一步的复杂度低，AI出错概率小，总体效率更高，也便于定位和解决问题。
第四，AI在性能优化方面存在局限。AI在功能实现方面表现良好，但在性能优化方面往往需要人工深入分析根因。例如万级数据导入页面卡死的问题，AI最初给出的"增加加载提示"等建议没有解决根本问题，需要人工分析出主线程阻塞和Vue响应式开销的根因，再指导AI采用正确的优化方案。这说明AI在需要深度系统分析和性能调优的场景中，仍需人工主导。
第五，AI辅助编程改变了开发模式但不改变核心能力要求。Vibe Coding模式下，开发者更多地扮演"架构师"和"审核者"的角色，负责需求拆解、方案设计、代码审核和问题定位，而具体的代码编写工作更多地交给AI完成。但这并不意味着开发者不需要掌握编程技能，相反，只有具备扎实的编程基础和系统设计能力，才能有效地引导AI、审核AI输出、发现AI错误并指导AI修正。AI是放大器，它能放大优秀开发者的能力，但不能替代开发者的核心能力。

# 第七章 总结与展望
## 7.1 研究总结
本课程设计针对机械设备预测性维护的实际需求，设计并实现了一套基于多智能算法的机械设备故障预测与运维决策系统。主要完成了以下工作：
第一，完成了数据资源构建。以NASA C-MAPSS涡扇发动机退化数据集为研究对象，进行了数据清洗、特征选择、数据标准化和RUL标签构建等预处理工作。通过方差分析和相关性分析，从21个传感器特征中筛选出14个关键特征，采用Z-Score标准化消除量纲差异，采用分段线性RUL标签构建方法更符合设备实际退化规律。构建了包含29436条样本的高质量数据集，为模型训练奠定了基础。
第二，完成了预测算法设计。采用随机森林算法构建了故障分类模型和剩余使用寿命预测模型。故障分类模型能够准确识别四种故障模式，测试集准确率达到92%；RUL预测模型能够定量预测设备剩余使用寿命，平均绝对误差为12.5个周期，决定系数R²达到0.87。模型使用joblib序列化保存，系统启动时预加载到内存，推理速度快。
第三，完成了系统方案设计。采用前后端分离的B/S架构，设计了表现层、业务层、算法层和数据层四层架构，划分了数据导入、故障诊断、寿命预测、运维决策和历史记录五大功能模块，设计了SQLite数据库表结构和RESTful API接口规范，确保系统具有良好的可扩展性和可维护性。
第四，完成了系统开发实现。前端基于Vue3框架配合Element Plus和ECharts开发，实现了可视化交互界面；后端基于FastAPI框架开发，提供RESTful API服务；采用SQLite数据库存储分析结果。针对万级数据导入导致的前端卡顿问题，采用Web Worker后台解析、分块传输、远程搜索下拉框和非响应式数据等优化策略，实现了13096条数据的秒级导入和10秒内的批量分析。
第五，完成了系统测试与集成调试。开展了功能测试和性能测试，15项功能测试用例全部通过，系统各功能模块运行正常；性能测试表明系统在处理万级数据时表现优异，批量诊断响应时间6.5秒，满足10秒以内的要求。在集成调试中解决了接口格式、跨域、模型加载、前端性能和数据库操作等问题，系统各模块协同工作稳定。
第六，完成了AI使用披露。本项目采用Vibe Coding（AI辅助编程）模式开发，第六章详细披露了所用AI工具、关键prompt策略、5个典型AI出错案例及纠正过程，以及对AI辅助编程的反思和体会，体现了对AI工具使用的透明性和批判性思考。
## 7.2 创新点
本课程设计的主要创新点体现在以下几个方面：
第一，双模型协同的预测框架。将随机森林分类算法和回归算法有机结合，同时实现故障类型识别和剩余使用寿命预测，形成了从故障诊断到寿命预测的完整预测链条，为设备维护决策提供全面的信息支撑。两个模型共享相同的输入特征和标准化流程，推理时可并行计算，提高了分析效率。
第二，面向万级数据的前端性能优化方案。针对工业设备数据量大的特点，提出了一套完整的前端性能优化方案，包括Web Worker后台线程解析、分块传输、远程搜索下拉框和非响应式数据存储等技术，有效解决了万级数据导入和展示导致的页面卡顿问题。该方案不仅适用于本项目，也为其他工业大数据可视化应用提供了参考。
第三，轻量化的预测性维护解决方案。采用CDN引入前端依赖、单文件架构、SQLite零配置数据库等设计，系统部署简单、运行资源占用低，普通笔记本电脑即可运行，降低了预测性维护技术的应用门槛，适合中小企业的实际需求和课程设计的演示场景。
第四，完整的AI使用披露。在课程设计报告中专门设立AI使用披露章节，详细记录AI工具的使用情况、prompt策略、出错案例和反思体会，体现了对AI辅助编程这一新兴开发模式的深入思考和透明披露，符合当前对AI使用透明度的要求。
## 7.3 不足与展望
尽管本系统取得了一定的研究成果，但仍存在一些不足之处，需要在后续工作中进一步改进和完善：
第一，算法方面。本系统采用的随机森林算法虽然具有良好的预测精度，但在处理长时序数据时未能充分利用时序信息。后续研究可引入长短期记忆网络（LSTM）、Transformer等深度学习模型，更好地捕捉设备退化的时序特征，进一步提高预测精度。同时，可研究多算法集成方法，融合多种算法的优势，提高模型的鲁棒性和泛化能力。还可研究自动化特征工程方法，减少对专家经验的依赖。
第二，数据方面。本系统仅使用了C-MAPSS单一数据集进行验证，数据集场景较为单一（航空发动机）。后续研究可收集更多类型的机械设备数据，如轴承、齿轮箱、电机、泵类等，构建多场景的设备故障预测模型，提高系统的适用性和泛化能力。同时，可研究数据增强和迁移学习方法，解决真实工业数据标注困难和样本不平衡的问题。
第三，功能方面。本系统目前主要实现了故障诊断和寿命预测功能，运维决策功能较为简单（基于启发式规则）。后续可增加设备健康度综合评估、维护计划自动排程、备件库存管理、维护成本分析、维修知识库等功能，构建更加完善的设备全生命周期管理平台。同时，可增加实时数据接入功能，支持通过OPC UA、MQTT、Modbus等工业协议实时采集设备运行数据，实现真正的在线预测性维护，而不仅仅是离线CSV数据分析。
第四，性能方面。虽然本系统在万级数据量下表现良好，但在更大数据量（十万级、百万级）场景下仍有优化空间。后续可研究分布式计算和GPU加速技术，提高批量推理速度；可引入Redis等内存数据库缓存热点数据和模型推理结果，提高查询性能；可采用数据压缩和列式存储技术，降低存储开销和IO延迟；可研究模型压缩和量化技术，减小模型文件体积，提高推理速度。
第五，部署方面。本系统目前采用本地部署方式，后续可研究容器化部署方案，使用Docker打包应用，支持一键部署和弹性扩缩容。同时，可研究云原生架构，将系统部署到云平台，支持多用户并发访问和远程运维。还可增加用户认证和权限管理功能，支持多租户使用场景。
第六，AI辅助编程方面。本项目采用Vibe Coding模式开发，AI工具在提高开发效率的同时也暴露出一些局限性。后续可研究更有效的AI辅助编程方法，如AI代码审查自动化、AI生成代码的安全性检测、AI与传统开发工具的深度集成等。同时，应持续关注AI技术的发展，及时将新的AI工具和方法应用到开发实践中，不断提高开发效率和代码质量。

# 参考文献
[1] 张英, 王建华, 李刚. 机械设备故障诊断技术研究综述[J]. 机械工程学报, 2022, 58(10): 1-15.
[2] 刘志强, 陈明. 基于深度学习的设备剩余使用寿命预测方法研究[J]. 自动化学报, 2023, 49(3): 512-525.
[3] 王伟, 赵磊. 预测性维护技术在智能制造中的应用研究[J]. 计算机集成制造系统, 2022, 28(7): 2015-2028.
[4] 李明, 孙浩. 基于随机森林的旋转机械故障诊断方法[J]. 振动与冲击, 2021, 40(15): 128-135.
[5] 陈涛, 周杰. C-MAPSS数据集在航空发动机prognostics中的应用综述[J]. 航空学报, 2023, 44(2): 120-138.
[6] 杨帆, 黄磊. 工业互联网平台架构与关键技术研究[J]. 计算机研究与发展, 2022, 59(1): 1-16.
[7] 赵鹏, 吴迪. 基于FastAPI的高性能Web服务设计与实现[J]. 计算机工程与设计, 2023, 44(4): 1023-1030.
[8] 孙丽, 郑伟. Vue3框架在前端开发中的应用研究[J]. 软件工程, 2022, 25(8): 45-49.
[9] 马骏, 钱程. 基于ECharts的数据可视化系统设计与实现[J]. 计算机应用与软件, 2023, 40(1): 156-162.
[10] 周明, 吴刚. 设备剩余使用寿命预测中的数据预处理方法研究[J]. 仪器仪表学报, 2021, 42(9): 88-97.
[11] Breiman L. Random Forests[J]. Machine Learning, 2001, 45(1): 5-32.
[12] Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]. International Conference on Prognostics and Health Management, 2008: 1-9.
[13] Heimes F O. Recurrent neural networks for remaining useful life estimation[C]. International Conference on Prognostics and Health Management, 2008: 1-6.
[14] Wang T, Yu J, Siegel D, et al. A similarity-based prognostics approach for remaining useful life estimation of engineered systems[C]. International Conference on Prognostics and Health Management, 2008: 1-6.
[15] Ramasso E, Saxena A. Performance benchmarking and analysis of prognostic methods for CMAPSS datasets[J]. International Journal of Prognostics and Health Management, 2014, 5(2): 1-15.

| 章节 | 主要内容 |
| --- | --- |
| 第一章 背景调研 | 研究背景、研究意义（经济 / 技术 / 应用价值）、国内外研究现状 |
| 第二章 方案设计 | 需求分析、总体架构设计（四层 B/S 架构）、功能模块设计（5 大模块）、数据库设计（5 张表）、接口设计（7 个 API） |
| 第三章 数据资源构建 | C-MAPSS 数据集介绍、数据预处理（清洗 / 特征选择 / 标准化）、RUL 标签构建（分段线性）、数据资源管理 |
| 第四章 系统实现 | 开发环境、前端实现（6 个模块）、后端实现（6 项核心）、数据库实现、故障分类算法模块、RUL 预测算法模块、运维决策算法模块 |
| 第五章 测试与集成调试 | 测试环境、功能测试（15 项用例）、性能测试（6 项指标）、集成调试（5 个问题的解决过程）、测试结果分析 |
| 第六章 AI 使用披露（新增） | 所用 AI 工具、5 大关键 prompt 策略、5 个 AI 出错案例及纠正过程、AI 使用反思 |
| 第七章 总结与展望 | 研究总结（6 项工作）、创新点（4 项）、不足与展望（6 个方向） |


| 字段名 | 数据类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | 主键，自增 | 记录编号 |
| device_id | VARCHAR(50) | 非空 | 设备编号 |
| cycle | INTEGER | 非空 | 运行周期 |
| fault_type | VARCHAR(50) | 非空 | 故障类型 |
| confidence | FLOAT | 非空 | 预测置信度 |
| created_at | TIMESTAMP | 非空 | 诊断时间 |


| 接口路径 | 请求方法 | 功能说明 | 请求参数 |
| --- | --- | --- | --- |
| /api/health | GET | 健康检查 | 无 |
| /api/fault/predict | POST | 单样本故障诊断 | features: 14维特征数组 |
| /api/fault/batch | POST | 批量故障诊断 | samples: 样本数组 |
| /api/rul/predict | POST | 剩余寿命预测 | features: 14维特征数组 |
| /api/maintenance | POST | 运维决策建议 | fault_type, rul, device_id |
| /api/history | GET | 历史记录查询 | device_id, start_date, end_date |
| /api/history/{id} | DELETE | 删除历史记录 | id: 记录编号 |


| 指标 | 训练集 | 测试集 | 合计 |
| --- | --- | --- | --- |
| 设备数量 | 100 | 100 | 200 |
| 样本数量 | 16,340 | 13,096 | 29,436 |
| 原始特征数 | 21 | 21 | 21 |
| 筛选后特征数 | 14 | 14 | 14 |


| 序号 | 特征名 | 物理含义 | 序号 | 特征名 | 物理含义 |
| --- | --- | --- | --- | --- | --- |
| 1 | s2 | 低压转子转速 | 8 | s12 | 高压压气机出口静压 |
| 2 | s3 | 高压转子转速 | 9 | s13 | 风扇出口静压 |
| 3 | s4 | 燃油流量 | 10 | s14 | 高压涡轮出口总压 |
| 4 | s7 | 高压压气机出口总压 | 11 | s15 | 燃油/空气比 |
| 5 | s8 | 高压压气机出口静压 | 12 | s17 | 高压涡轮冷却空气流量 |
| 6 | s9 | 风扇出口总压 | 13 | s20 | 低压涡轮冷却空气流量 |
| 7 | s11 | 高压压气机出口静温 | 14 | s21 | 低压涡轮出口温度 |


| 类别 | 名称 | 版本 |
| --- | --- | --- |
| 操作系统 | Windows | 10/11 64位 |
| 开发语言 | Python | 3.10+ |
| 后端框架 | FastAPI | 0.100+ |
| ASGI服务器 | Uvicorn | 0.23+ |
| 机器学习库 | scikit-learn | 1.3+ |
| 数据处理库 | Pandas | 2.0+ |
| 模型序列化 | joblib | 1.3+ |
| 前端框架 | Vue3 | 3.3+ |
| UI组件库 | Element Plus | 2.3+ |
| 图表库 | ECharts | 5.4+ |
| 数据库 | SQLite | 3.30+ |
| 浏览器 | Chrome/Edge | 最新版 |


| 编号 | 测试模块 | 测试内容 | 预期结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- | --- | --- |
| FT01 | 数据导入 | 上传正常CSV文件 | 成功解析并显示统计信息 | 成功解析，显示13096条 | 通过 |
| FT02 | 数据导入 | 上传非CSV格式文件 | 提示文件格式错误 | 提示请上传CSV文件 | 通过 |
| FT03 | 数据导入 | 上传空文件 | 提示文件为空 | 提示文件内容为空 | 通过 |
| FT04 | 故障诊断 | 选择有效样本进行单样本诊断 | 返回故障类型和置信度 | 正常返回结果 | 通过 |
| FT05 | 故障诊断 | 批量诊断全部样本 | 返回所有样本的诊断结果 | 13096条全部诊断完成 | 通过 |
| FT06 | 故障诊断 | 未导入数据时点击诊断 | 提示请先导入数据 | 提示请先导入数据 | 通过 |
| FT07 | 寿命预测 | 选择有效设备进行RUL预测 | 返回RUL数值和趋势图 | 正常返回RUL值 | 通过 |
| FT08 | 寿命预测 | RUL数值显示 | 数值在合理范围内 | RUL值0-125之间 | 通过 |
| FT09 | 运维决策 | 根据诊断结果生成建议 | 显示风险等级和维护建议 | 正常显示决策建议 | 通过 |
| FT10 | 运维决策 | 风险等级颜色标识 | 四级风险对应四种颜色 | 颜色标识正确 | 通过 |
| FT11 | 历史记录 | 查询全部历史记录 | 显示所有历史分析记录 | 正常显示记录列表 | 通过 |
| FT12 | 历史记录 | 按设备编号筛选 | 只显示指定设备的记录 | 筛选结果正确 | 通过 |
| FT13 | 历史记录 | 删除单条记录 | 记录从列表中移除 | 删除成功 | 通过 |
| FT14 | 系统 | 后端服务启动 | 服务在8000端口正常运行 | 服务启动成功 | 通过 |
| FT15 | 系统 | 健康检查接口 | 返回服务正常状态 | 返回status: ok | 通过 |


| 测试项 | 数据量 | 平均响应时间 | 性能要求 | 是否达标 |
| --- | --- | --- | --- | --- |
| 数据导入 | 13096条 | 2.3秒 | < 5秒 | 达标 |
| 单样本故障诊断 | 1条 | 0.08秒 | < 1秒 | 达标 |
| 批量故障诊断 | 13096条 | 6.5秒 | < 10秒 | 达标 |
| RUL预测 | 1条 | 0.06秒 | < 1秒 | 达标 |
| 历史记录查询 | 1000条 | 0.12秒 | < 1秒 | 达标 |
| 页面加载 | 完整页面 | 1.5秒 | < 3秒 | 达标 |


| 工具名称 | 类型 | 主要用途 | 使用频率 |
| --- | --- | --- | --- |
| 豆包AI助手 | 大语言模型 | 代码生成、调试排错、文档撰写、方案设计 | 全程高频使用 |
| GitHub Copilot | 代码补全工具 | 代码自动补全、函数签名建议 | 代码编写阶段使用 |
| ECharts官方文档AI | 文档助手 | 图表配置查询、API用法说明 | 前端图表开发使用 |
