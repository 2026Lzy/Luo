# 基于多智能算法的机械设备故障预测与运维决策系统 — 需求报告说明

## 一、项目概述

### 1.1 项目背景

随着工业4.0和智能制造的快速发展，机械设备的可靠性和运维效率成为企业核心竞争力的重要组成部分。传统的"事后维修"和"定期维护"模式存在维修成本高、设备停机时间长、故障预警能力不足等问题。预测性维护（Predictive Maintenance）通过实时监测设备状态、分析传感器数据、预测故障发生时间，能够在故障发生前主动安排维护，显著降低运维成本和停机损失。

本项目旨在构建一个基于多智能算法的机械设备故障预测与运维决策系统，集成故障分类、剩余使用寿命（RUL）预测和运维决策三大核心功能，为机械设备的智能化运维提供完整的技术解决方案。

### 1.2 项目目标

- 构建一个B/S架构的Web应用系统，支持浏览器端访问
- 实现基于随机森林算法的故障类型分类，准确率达到90%以上
- 实现基于随机森林回归的剩余使用寿命（RUL）定量预测，MAE小于15个周期
- 实现基于故障类型和RUL的二维风险评估矩阵，生成四级风险等级和配套维护建议
- 支持万级（13096条）传感器数据的批量导入和秒级分析
- 系统在普通笔记本电脑上可稳定运行，单样本诊断响应时间小于1秒

### 1.3 项目范围

**包含范围：**
- 数据导入与管理模块（CSV文件上传、数据预览、批量导入）
- 故障诊断模块（单样本手动输入诊断、批量数据导入诊断）
- 寿命预测模块（RUL定量预测、寿命等级划分、趋势可视化）
- 运维决策模块（风险等级评估、维护建议生成、优先级排序）
- 历史记录模块（诊断/预测记录查询、筛选、详情查看、数据导出）
- 系统设置模块（API地址配置、主题切换、系统信息）

**不包含范围：**
- 传感器硬件接入和实时数据流采集
- 用户注册登录和多租户权限管理
- 移动端APP开发
- 深度学习模型（LSTM/Transformer等）的训练和部署

---

## 二、用户角色与使用场景

### 2.1 用户角色

| 角色 | 描述 | 核心操作 |
|------|------|----------|
| 设备运维工程师 | 负责设备日常巡检和维护执行 | 数据导入、故障诊断、查看运维建议、记录维护结果 |
| 设备管理人员 | 负责设备运维计划制定和资源调度 | 批量诊断、RUL预测、风险评估、维护优先级排序、历史记录分析 |
| 系统管理员 | 负责系统配置和数据管理 | API配置、数据库管理、数据备份与恢复 |

### 2.2 典型使用场景

**场景一：单设备快速诊断**
运维工程师在现场巡检时，手动输入14个传感器读数，系统在1秒内返回故障类型、置信度和维护建议，工程师根据结果决定是否需要进一步检查或安排维护。

**场景二：批量设备状态评估**
设备管理人员定期导出所有设备的传感器数据（CSV格式，最多13096条），上传到系统进行批量诊断，系统在10秒内返回所有设备的故障状态和RUL预测结果，管理人员根据风险等级排序制定下周维护计划。

**场景三：设备寿命趋势跟踪**
设备管理人员选择特定设备，查看其RUL预测历史趋势曲线，判断设备退化速度，提前安排备件采购和维护窗口，避免突发故障导致的生产线停机。

---

## 三、功能需求

### 3.1 数据导入模块（FR-01）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-01-01 | 支持CSV格式传感器数据文件上传，文件大小不超过50MB | 高 |
| FR-01-02 | 上传过程中显示实时进度条，支持取消上传 | 高 |
| FR-01-03 | 上传完成后显示数据预览（前20行），包含列名和数据类型 | 高 |
| FR-01-04 | 支持13096条及以上数据的批量导入，导入时间不超过10秒 | 高 |
| FR-01-05 | 导入过程中页面不卡顿、不白屏，可正常操作其他功能 | 高 |
| FR-01-06 | 支持引擎编号远程搜索下拉框，不一次性加载全部选项 | 中 |
| FR-01-07 | 数据格式校验：检查必填列是否存在、数据类型是否正确、缺失值比例 | 中 |
| FR-01-08 | 导入失败时显示具体错误信息（行号、错误原因），支持修正后重新上传 | 中 |

### 3.2 故障诊断模块（FR-02）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-02-01 | 支持单样本手动输入：14个传感器读数的表单输入，带输入验证 | 高 |
| FR-02-02 | 支持批量数据导入诊断：从已导入的数据中选择样本进行批量诊断 | 高 |
| FR-02-03 | 输出故障类型分类结果（正常、风扇故障、压气机故障、涡轮故障等） | 高 |
| FR-02-04 | 输出各类别概率（置信度），显示最高概率类别及其概率值 | 高 |
| FR-02-05 | 单样本诊断响应时间不超过1秒 | 高 |
| FR-02-06 | 13096条批量诊断响应时间不超过10秒 | 高 |
| FR-02-07 | 诊断结果自动保存到历史记录，可随时查询 | 中 |
| FR-02-08 | 支持诊断结果导出为CSV文件 | 低 |

### 3.3 寿命预测模块（FR-03）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-03-01 | 支持单样本RUL定量预测，输入14个传感器读数和运行周期 | 高 |
| FR-03-02 | 输出RUL预测值（0-125个运行周期） | 高 |
| FR-03-03 | RUL预测值按阈值划分为四个等级：健康（>100）、关注（50-100）、警告（20-50）、危险（<20） | 高 |
| FR-03-04 | 不同等级用不同颜色标识（绿色/黄色/橙色/红色） | 中 |
| FR-03-05 | 支持RUL历史趋势曲线可视化（ECharts折线图） | 中 |
| FR-03-06 | 单样本RUL预测响应时间不超过1秒 | 高 |
| FR-03-07 | 预测结果自动保存到历史记录 | 中 |

### 3.4 运维决策模块（FR-04）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-04-01 | 基于故障类型和RUL的二维风险评估矩阵，输出四级风险等级 | 高 |
| FR-04-02 | 风险等级定义：低风险（绿色）、中风险（黄色）、高风险（橙色）、紧急（红色） | 高 |
| FR-04-03 | 根据风险等级生成配套维护建议（继续运行/增加监测/尽快维护/立即停机） | 高 |
| FR-04-04 | 显示决策依据：故障类型、RUL值、风险等级、建议措施 | 中 |
| FR-04-05 | 支持多设备维护优先级排序（按风险等级和RUL值排序） | 中 |
| FR-04-06 | 运维决策响应时间不超过1秒 | 高 |

### 3.5 历史记录模块（FR-05）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-05-01 | 支持诊断记录和预测记录的列表展示，分页加载 | 高 |
| FR-05-02 | 支持按时间范围筛选记录 | 中 |
| FR-05-03 | 支持按设备编号/引擎编号筛选记录 | 中 |
| FR-05-04 | 支持按故障类型/风险等级筛选记录 | 中 |
| FR-05-05 | 点击记录可查看完整详情（输入参数、输出结果、时间戳） | 高 |
| FR-05-06 | 支持记录数据导出为CSV文件 | 低 |
| FR-05-07 | 支持单条记录删除和批量删除 | 低 |

### 3.6 系统设置模块（FR-06）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-06-01 | 支持后端API地址配置（默认http://localhost:8000） | 中 |
| FR-06-02 | 支持前端主题切换（浅色/深色） | 低 |
| FR-06-03 | 显示系统信息（版本号、技术栈、模型文件状态） | 低 |
| FR-06-04 | 支持一键启动后端服务（start.bat脚本） | 中 |

---

## 四、非功能需求

### 4.1 性能需求（NFR-01）

| 指标 | 要求 | 测试条件 |
|------|------|----------|
| 单样本故障诊断响应时间 | ≤ 1秒 | 普通笔记本电脑，1条样本 |
| 单样本RUL预测响应时间 | ≤ 1秒 | 普通笔记本电脑，1条样本 |
| 运维决策响应时间 | ≤ 1秒 | 普通笔记本电脑，1次决策 |
| 万级数据导入时间 | ≤ 10秒 | 13096条CSV数据 |
| 万级批量诊断时间 | ≤ 10秒 | 13096条样本批量诊断 |
| 前端页面加载时间 | ≤ 3秒 | 首页首次加载 |
| 系统并发支持 | ≥ 5个并发用户 | 同时进行诊断操作 |
| 内存占用 | ≤ 2GB | 系统稳定运行时 |

### 4.2 可用性需求（NFR-02）

- 系统界面简洁直观，运维工程师无需培训即可完成基本操作
- 所有操作提供明确的反馈（加载状态、成功提示、错误提示）
- 错误提示信息清晰易懂，包含错误原因和建议操作
- 支持一键启动（start.bat），无需复杂的环境配置
- 系统在普通笔记本电脑（8GB内存，i5处理器）上可稳定运行

### 4.3 可靠性需求（NFR-03）

- 系统全年可用率不低于95%
- 数据导入失败时不影响已有数据，支持断点续传
- 后端服务异常时前端显示友好提示，不白屏不崩溃
- 数据库支持自动备份（每日一次），数据丢失不超过24小时
- 模型文件加载失败时给出明确错误提示，不静默失败

### 4.4 可维护性需求（NFR-04）

- 代码结构清晰，模块化设计，前后端分离
- 关键代码添加注释，复杂算法提供文档说明
- 模型文件可替换，无需修改代码即可升级模型
- API接口遵循RESTful规范，提供接口文档
- 数据库表结构设计合理，支持后续扩展

### 4.5 安全性需求（NFR-05）

- 前后端通信使用HTTP协议（本地部署场景），生产环境应升级为HTTPS
- 输入数据进行格式验证和类型检查，防止非法数据注入
- 数据库文件设置访问权限，防止未授权访问
- 系统不收集用户个人信息，所有数据本地存储
- API接口添加基本的请求频率限制，防止滥用

### 4.6 兼容性需求（NFR-06）

- 前端支持Chrome、Edge、Firefox等主流浏览器的最新两个版本
- 后端支持Windows 10/11操作系统
- Python版本要求3.8及以上
- 支持CSV文件的UTF-8和GBK编码自动识别

---

## 五、数据需求

### 5.1 输入数据格式

系统接受CSV格式的传感器数据，必须包含以下列：

| 列名 | 类型 | 说明 | 是否必填 |
|------|------|------|----------|
| engine_id | 整数 | 发动机/设备编号 | 是 |
| cycle | 整数 | 运行周期数 | 是 |
| s1-s21 | 浮点数 | 21个传感器测量值 | 是（系统自动筛选14个有效特征） |

**数据示例：**
```csv
engine_id,cycle,s1,s2,s3,s4,s5,s6,s7,s8,s9,s10,s11,s12,s13,s14,s15,s16,s17,s18,s19,s20,s21
1,1,0.0010,0.0000,100.0,518.67,641.82,1589.70,1400.60,14.62,21.61,554.36,2388.06,9046.19,1.30,47.47,521.66,2388.02,8138.62,8.4195,0.03,392,2046
```

### 5.2 数据库设计

系统使用SQLite数据库，包含以下5张核心表：

**1. equipment（设备信息表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| engine_id | INTEGER | 设备编号，唯一 |
| model | TEXT | 设备型号 |
| install_date | DATE | 安装日期 |
| status | TEXT | 设备状态（正常/维护中/故障） |
| created_at | DATETIME | 创建时间 |

**2. sensor_data（传感器数据表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| engine_id | INTEGER | 设备编号，外键 |
| cycle | INTEGER | 运行周期 |
| s1-s21 | REAL | 21个传感器读数 |
| timestamp | DATETIME | 数据采集时间 |
| created_at | DATETIME | 导入时间 |

**3. fault_record（故障记录表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| engine_id | INTEGER | 设备编号 |
| fault_type | TEXT | 故障类型 |
| confidence | REAL | 置信度（0-1） |
| severity | TEXT | 严重程度（低/中/高/紧急） |
| detected_at | DATETIME | 检测时间 |
| created_at | DATETIME | 记录时间 |

**4. prediction（预测结果表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| engine_id | INTEGER | 设备编号 |
| rul_value | INTEGER | RUL预测值（周期数） |
| rul_level | TEXT | 寿命等级（健康/关注/警告/危险） |
| confidence | REAL | 预测置信度 |
| predicted_at | DATETIME | 预测时间 |
| created_at | DATETIME | 记录时间 |

**5. maintenance（维护记录表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| engine_id | INTEGER | 设备编号 |
| maintenance_type | TEXT | 维护类型（例行/故障/预防性） |
| description | TEXT | 维护描述 |
| risk_level | TEXT | 维护时风险等级 |
| maintained_at | DATETIME | 维护时间 |
| maintained_by | TEXT | 维护人员 |
| created_at | DATETIME | 记录时间 |

### 5.3 模型文件

系统依赖两个预训练的机器学习模型文件：

| 文件名 | 大小 | 内容 | 用途 |
|--------|------|------|------|
| fault_classifier.pkl | 7.27MB | {"model": RandomForestClassifier, "scaler": StandardScaler} | 故障类型分类 |
| rul_predictor.pkl | 88.09MB | {"model": RandomForestRegressor, "scaler": StandardScaler} | RUL定量预测 |

模型文件使用Python的pickle格式序列化，包含训练好的模型和对应的标准化器（scaler），确保推理时数据预处理与训练时一致。

---

## 六、接口需求

### 6.1 API接口列表

系统后端提供以下7个RESTful API接口：

| 接口路径 | 方法 | 功能 | 请求参数 | 响应格式 |
|----------|------|------|----------|----------|
| /api/data/upload | POST | CSV数据文件上传 | file (multipart/form-data) | {success, count, preview} |
| /api/data/list | GET | 获取已导入数据列表 | engine_id, page, page_size | {data, total, page} |
| /api/diagnose/single | POST | 单样本故障诊断 | features (14维数组) | {fault_type, confidence, probabilities} |
| /api/diagnose/batch | POST | 批量故障诊断 | data (样本列表) | {results, total, success_count} |
| /api/predict/rul | POST | 剩余使用寿命预测 | features, engine_id, cycle | {rul_value, rul_level, confidence} |
| /api/maintenance/decision | POST | 运维决策建议生成 | fault_type, rul_value, engine_id | {risk_level, suggestion, priority, reason} |
| /api/history | GET | 历史记录查询 | type, engine_id, start_date, end_date, page | {records, total, page} |

### 6.2 接口详细说明

**1. 单样本故障诊断接口**
- 请求示例：
```json
{
  "features": [641.82, 1589.70, 1400.60, 554.36, 2388.06, 9046.19, 47.47, 521.66, 2388.02, 8138.62, 8.4195, 0.03, 392, 2046]
}
```
- 响应示例：
```json
{
  "fault_type": "正常",
  "confidence": 0.92,
  "probabilities": {
    "正常": 0.92,
    "风扇故障": 0.03,
    "压气机故障": 0.03,
    "涡轮故障": 0.02
  }
}
```

**2. RUL预测接口**
- 请求示例：
```json
{
  "features": [641.82, 1589.70, 1400.60, 554.36, 2388.06, 9046.19, 47.47, 521.66, 2388.02, 8138.62, 8.4195, 0.03, 392, 2046],
  "engine_id": 1,
  "cycle": 50
}
```
- 响应示例：
```json
{
  "rul_value": 85,
  "rul_level": "关注",
  "confidence": 0.88
}
```

**3. 运维决策接口**
- 请求示例：
```json
{
  "fault_type": "压气机故障",
  "rul_value": 30,
  "engine_id": 1
}
```
- 响应示例：
```json
{
  "risk_level": "高风险",
  "suggestion": "尽快安排维护（1-2周内），降低设备负载，增加监测频率",
  "priority": 2,
  "reason": "检测到压气机故障，剩余使用寿命30个周期，处于警告区间"
}
```

### 6.3 通用响应格式

所有接口统一使用以下响应格式：
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

错误响应：
```json
{
  "code": 400,
  "message": "参数错误：features必须是14维数组",
  "data": null
}
```

---

## 七、约束条件

### 7.1 技术约束

- 前端框架：Vue 3 + Vite + Element Plus + ECharts
- 后端框架：Python 3.8+ + FastAPI + Uvicorn
- 机器学习：scikit-learn（随机森林算法）
- 数据库：SQLite（轻量级，无需独立服务）
- 操作系统：Windows 10/11
- 浏览器：Chrome/Edge/Firefox最新两个版本

### 7.2 数据约束

- 输入数据必须为CSV格式，UTF-8或GBK编码
- 单文件大小不超过50MB
- 单次批量诊断不超过20000条样本
- 传感器数据必须包含engine_id和cycle列
- 14个有效特征列（s2-s21中的特定列）必须存在且为数值类型

### 7.3 性能约束

- 系统必须在普通笔记本电脑（8GB内存，i5处理器）上稳定运行
- 内存占用不超过2GB
- 单样本诊断响应时间不超过1秒
- 万级数据批量分析不超过10秒

### 7.4 部署约束

- 系统支持本地单机部署，无需云服务器
- 一键启动脚本（start.bat）自动启动后端服务
- 前端可通过npm run dev启动开发服务器，或npm run build打包为静态文件
- 模型文件必须与后端代码在同一目录结构中

---

## 八、验收标准

### 8.1 功能验收

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 数据导入 | 支持CSV上传，13096条数据10秒内导入完成，页面不卡顿 | 上传test.csv，计时并观察页面响应 |
| 单样本诊断 | 输入14个传感器值，1秒内返回故障类型和置信度 | 手动输入测试样本，验证结果 |
| 批量诊断 | 13096条数据批量诊断，10秒内完成，返回所有结果 | 选择已导入数据，执行批量诊断 |
| RUL预测 | 输入传感器值和周期，1秒内返回RUL值和等级 | 手动输入测试样本，验证预测结果 |
| 运维决策 | 输入故障类型和RUL，返回风险等级和维护建议 | 组合不同输入，验证决策逻辑 |
| 历史记录 | 诊断/预测结果自动保存，可按条件筛选查询 | 执行多次诊断，验证记录和筛选 |
| 系统设置 | API地址可配置，保存后生效 | 修改API地址，验证请求目标 |

### 8.2 性能验收

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 单样本诊断响应 | ≤ 1秒 | 连续测试10次，取平均值 |
| 单样本RUL预测响应 | ≤ 1秒 | 连续测试10次，取平均值 |
| 万级数据导入 | ≤ 10秒 | 上传13096条test.csv，计时 |
| 万级批量诊断 | ≤ 10秒 | 对13096条数据执行批量诊断，计时 |
| 页面加载 | ≤ 3秒 | 清空缓存后首次加载首页，计时 |
| 内存占用 | ≤ 2GB | 系统稳定运行时查看任务管理器 |

### 8.3 可靠性验收

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 异常输入处理 | 输入非法数据时显示友好提示，不崩溃 | 输入空值、错误类型、超范围值 |
| 后端异常处理 | 后端服务停止时前端显示提示，不白屏 | 停止后端服务，操作前端功能 |
| 大文件处理 | 上传超过50MB文件时提示文件过大 | 上传大文件，验证提示信息 |
| 数据完整性 | 导入失败时不丢失已有数据 | 中途取消上传，验证已有数据 |

### 8.4 文档验收

| 验收项 | 验收标准 |
|--------|----------|
| README.md | 包含项目介绍、技术栈、安装步骤、使用说明、目录结构 |
| 需求报告说明.md | 包含功能需求、非功能需求、数据需求、接口需求、验收标准 |
| 方案设计.md | 包含架构设计、模块设计、数据库设计、算法设计 |
| API接口文档 | 包含所有接口的请求参数、响应格式、示例代码 |
| 代码注释 | 关键函数和复杂逻辑添加注释，注释覆盖率≥30% |

---

## 九、项目里程碑

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| 需求分析 | 第1周 | 需求报告说明.md、选题说明.md |
| 方案设计 | 第2周 | 方案设计.md、系统架构图、数据库设计图 |
| 数据准备 | 第3周 | C-MAPSS数据集、数据预处理脚本、训练好的模型文件 |
| 后端开发 | 第4-5周 | FastAPI后端代码、7个API接口、数据库操作 |
| 前端开发 | 第6-7周 | Vue3前端代码、6个功能页面、万级数据优化 |
| 算法集成 | 第8周 | 故障分类模型、RUL预测模型、运维决策规则引擎 |
| 测试调试 | 第9周 | 功能测试报告、性能测试报告、Bug修复 |
| 文档撰写 | 第10周 | 课程设计报告、答辩PPT、README完善 |
| 答辩验收 | 第11周 | 系统演示、答辩汇报 |

---

## 十、风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 模型准确率不达标 | 诊断结果不可靠 | 中 | 尝试多种算法（SVM/XGBoost），调整超参数，增加特征工程 |
| 万级数据导入卡顿 | 用户体验差 | 高 | Web Worker分块解析、分块传输、非响应式数据、虚拟滚动 |
| 模型文件过大加载慢 | 启动时间长 | 中 | 模型压缩（剪枝/量化）、启动时预加载、懒加载 |
| 前后端联调问题 | 开发进度延迟 | 中 | 提前定义API接口规范，使用Mock数据并行开发 |
| 数据库性能瓶颈 | 查询响应慢 | 低 | 添加索引、分页查询、限制单次返回条数 |
| 浏览器兼容性问题 | 部分用户无法使用 | 低 | 限定支持的浏览器版本，使用Polyfill兼容旧浏览器 |

---

## 附录

### 附录A：术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| RUL | Remaining Useful Life | 剩余使用寿命，设备从当前到故障发生的运行时间 |
| C-MAPSS | Commercial Modular Aero-Propulsion System Simulation | 商用模块化航空推进系统仿真，NASA发布的涡扇发动机退化数据集 |
| 随机森林 | Random Forest | 一种集成学习算法，通过多棵决策树投票进行分类或回归 |
| 预测性维护 | Predictive Maintenance | 通过状态监测和故障预测，在故障发生前主动安排维护 |
| B/S架构 | Browser/Server | 浏览器/服务器架构，用户通过浏览器访问系统 |
| Z-Score标准化 | Z-Score Normalization | 将数据转换为均值为0、标准差为1的标准正态分布 |
| MAE | Mean Absolute Error | 平均绝对误差，预测值与真实值之差的绝对值的平均值 |
| RMSE | Root Mean Square Error | 均方根误差，预测值与真实值之差的平方的平均值的平方根 |

### 附录B：参考文献

1. Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]. International Conference on Prognostics and Health Management, 2008.
2. Breiman L. Random forests[J]. Machine Learning, 2001, 45(1): 5-32.
3. 张洪才, 何波. 机器学习导论[M]. 北京: 机械工业出版社, 2019.
4. 李航. 统计学习方法[M]. 北京: 清华大学出版社, 2019.
5. FastAPI官方文档: https://fastapi.tiangolo.com/
6. Vue3官方文档: https://vuejs.org/
7. scikit-learn官方文档: https://scikit-learn.org/

---

**文档版本：** v1.0
**创建日期：** 2026年9月3日
**最后更新：** 2026年9月3日
**编写人：** 罗志勇
