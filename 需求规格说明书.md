# 基于多智能算法的机械设备故障预测与运维决策系统
# 需求规格说明书（SRS）

**文档版本：** V1.0
**编制日期：** 2026年9月3日
**编制人：** 罗志勇
**项目名称：** 基于多智能算法的机械设备故障预测与运维决策系统

---

## 目录

1. [引言](#1-引言)
2. [总体描述](#2-总体描述)
3. [具体需求](#3-具体需求)
4. [外部接口需求](#4-外部接口需求)
5. [数据需求](#5-数据需求)
6. [非功能需求](#6-非功能需求)
7. [验收标准](#7-验收标准)
8. [项目约束](#8-项目约束)
9. [附录](#9-附录)

---

## 1. 引言

### 1.1 编写目的

本文档为"基于多智能算法的机械设备故障预测与运维决策系统"的软件需求规格说明书（Software Requirements Specification，SRS）。本文档旨在：

- 明确系统的功能需求和非功能需求，为系统设计、开发、测试和验收提供依据
- 描述系统的用户角色、使用场景和业务流程，确保开发团队与需求方对系统理解一致
- 定义系统的外部接口、数据格式和性能指标，为前后端开发和集成测试提供规范
- 作为项目验收的基准文档，确保最终交付的系统满足所有规定的需求

本文档的预期读者包括：项目指导教师、开发人员、测试人员、答辩评审专家。

### 1.2 项目背景

随着工业4.0和智能制造的快速发展，机械设备的可靠性和运维效率成为制造企业核心竞争力的重要组成部分。传统的设备维护模式主要包括：

- **事后维修（Breakdown Maintenance）：** 设备发生故障后才进行维修，存在维修成本高、停机时间长、可能引发安全事故等问题
- **定期维护（Preventive Maintenance）：** 按照固定时间间隔进行维护，存在过度维护（设备状态良好时仍维护）或维护不足（维护间隔内发生故障）等问题

预测性维护（Predictive Maintenance）通过实时监测设备运行状态、分析传感器数据、利用机器学习算法预测故障发生时间和剩余使用寿命（Remaining Useful Life，RUL），能够在故障发生前主动安排维护，显著降低运维成本和停机损失。

本项目旨在构建一个基于多智能算法的机械设备故障预测与运维决策系统，集成故障分类、RUL预测和运维决策三大核心功能，为机械设备的智能化运维提供完整的技术解决方案。

### 1.3 项目范围

**包含范围：**

| 模块 | 功能描述 |
|------|----------|
| 数据导入与管理 | CSV文件上传、数据预览、批量导入、数据校验 |
| 故障诊断 | 单样本手动输入诊断、批量数据导入诊断、故障类型分类、置信度计算 |
| 寿命预测 | RUL定量预测、寿命等级划分、趋势可视化 |
| 运维决策 | 风险等级评估、维护建议生成、优先级排序 |
| 历史记录 | 诊断/预测记录查询、筛选、详情查看、数据导出 |
| 系统设置 | API地址配置、主题切换、系统信息展示 |

**不包含范围：**

- 传感器硬件接入和实时数据流采集（MQTT/Kafka等）
- 用户注册登录和多租户权限管理
- 移动端APP开发
- 深度学习模型（LSTM/Transformer等）的训练和部署
- 云端部署和分布式计算

### 1.4 术语定义

| 术语/缩写 | 英文全称 | 中文说明 |
|-----------|----------|----------|
| RUL | Remaining Useful Life | 剩余使用寿命，设备从当前状态到故障发生的预计运行时间 |
| C-MAPSS | Commercial Modular Aero-Propulsion System Simulation | 商用模块化航空推进系统仿真，NASA发布的涡扇发动机退化数据集 |
| SRS | Software Requirements Specification | 软件需求规格说明书 |
| B/S | Browser/Server | 浏览器/服务器架构 |
| MAE | Mean Absolute Error | 平均绝对误差 |
| RMSE | Root Mean Square Error | 均方根误差 |
| API | Application Programming Interface | 应用程序编程接口 |
| CSV | Comma-Separated Values | 逗号分隔值文件格式 |
| ORM | Object Relational Mapping | 对象关系映射 |
| CORS | Cross-Origin Resource Sharing | 跨域资源共享 |

### 1.5 参考资料

1. Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]. International Conference on Prognostics and Health Management, 2008.
2. Breiman L. Random forests[J]. Machine Learning, 2001, 45(1): 5-32.
3. 李航. 统计学习方法（第2版）[M]. 北京: 清华大学出版社, 2019.
4. 张洪才, 何波. 机器学习导论[M]. 北京: 机械工业出版社, 2019.
5. IEEE Std 830-1998, IEEE Recommended Practice for Software Requirements Specifications.
6. FastAPI官方文档: https://fastapi.tiangolo.com/
7. Vue3官方文档: https://vuejs.org/
8. scikit-learn官方文档: https://scikit-learn.org/

---

## 2. 总体描述

### 2.1 产品愿景

本系统旨在为机械设备运维人员提供一个智能化、轻量化的故障预测与运维决策平台。通过集成随机森林故障分类模型、随机森林RUL预测模型和基于规则的运维决策引擎，系统能够：

- 快速识别设备故障类型，辅助运维人员进行故障定位
- 准确预测设备剩余使用寿命，帮助管理人员提前安排维护计划
- 基于故障类型和RUL生成风险评估和维护建议，实现从"被动维修"到"主动预测"的转变

系统采用B/S架构，前端基于Vue3开发，后端基于Python FastAPI开发，数据库使用SQLite，整体方案轻量化，可在普通笔记本电脑上稳定运行，适合中小企业和教学科研场景。

### 2.2 用户角色

| 角色 | 描述 | 核心操作 | 权限级别 |
|------|------|----------|----------|
| 设备运维工程师 | 负责设备日常巡检和维护执行 | 数据导入、单样本诊断、查看运维建议、记录维护结果 | 普通用户 |
| 设备管理人员 | 负责设备运维计划制定和资源调度 | 批量诊断、RUL预测、风险评估、维护优先级排序、历史记录分析 | 普通用户 |
| 系统管理员 | 负责系统配置和数据管理 | API配置、数据库管理、数据备份与恢复 | 管理员 |

**注：** 本系统为课程设计项目，暂不实现用户注册登录和权限管理功能，所有角色功能均对所有用户开放。

### 2.3 用户特征

- **设备运维工程师：** 具备机械设备基础知识，熟悉设备巡检流程，计算机操作水平一般，需要系统界面简洁直观、操作步骤简单
- **设备管理人员：** 具备设备管理经验，关注设备整体运行状态和维护成本，需要系统提供批量分析、趋势可视化和决策支持功能
- **系统管理员：** 具备计算机基础知识，负责系统部署和维护，需要系统提供配置管理和数据管理功能

### 2.4 运行环境

#### 2.4.1 硬件环境

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| 处理器 | Intel Core i5 或同等性能 | Intel Core i7 或同等性能 |
| 内存 | 8GB | 16GB |
| 硬盘 | 500MB可用空间 | 1GB可用空间 |
| 网络 | 本地运行无需网络 | 局域网连接 |

#### 2.4.2 软件环境

| 组件 | 要求 |
|------|------|
| 操作系统 | Windows 10 / Windows 11（64位） |
| Python | 3.8 及以上版本 |
| 浏览器 | Chrome 90+ / Edge 90+ / Firefox 88+ |
| Node.js | 14.0 及以上（前端开发环境） |
| Git | 2.0 及以上（版本管理） |

#### 2.4.3 依赖库

**后端Python依赖：**

| 库名 | 版本 | 用途 |
|------|------|------|
| fastapi | 0.100+ | Web框架 |
| uvicorn | 0.23+ | ASGI服务器 |
| scikit-learn | 1.3+ | 机器学习算法 |
| pandas | 2.0+ | 数据处理 |
| numpy | 1.24+ | 数值计算 |
| sqlalchemy | 2.0+ | ORM数据库操作 |
| pydantic | 2.0+ | 数据验证 |
| python-multipart | 0.0.6+ | 文件上传处理 |

**前端依赖：**

| 库名 | 版本 | 用途 |
|------|------|------|
| vue | 3.3+ | 前端框架 |
| vite | 4.0+ | 构建工具 |
| element-plus | 2.3+ | UI组件库 |
| echarts | 5.4+ | 数据可视化 |
| axios | 1.4+ | HTTP请求库 |
| pinia | 2.1+ | 状态管理 |
| vue-router | 4.2+ | 路由管理 |

### 2.5 系统架构

系统采用经典的四层B/S架构：

```
┌─────────────────────────────────────────────────────────┐
│                      表现层（前端）                        │
│  Vue3 + Element Plus + ECharts + Axios                   │
│  数据导入 | 故障诊断 | 寿命预测 | 运维决策 | 历史记录     │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP/RESTful API
┌──────────────────────────▼──────────────────────────────┐
│                      业务逻辑层（后端）                    │
│  Python + FastAPI + Uvicorn                               │
│  数据处理 | 故障诊断 | RUL预测 | 运维决策 | 数据库操作    │
└──────────────────────────┬──────────────────────────────┘
                           │ 模型加载/推理
┌──────────────────────────▼──────────────────────────────┐
│                      算法层（机器学习）                    │
│  scikit-learn RandomForest                                │
│  fault_classifier.pkl | rul_predictor.pkl                │
└──────────────────────────┬──────────────────────────────┘
                           │ SQLAlchemy ORM
┌──────────────────────────▼──────────────────────────────┐
│                      数据层（数据库）                      │
│  SQLite                                                    │
│  equipment | sensor_data | fault_record | prediction | maintenance │
└─────────────────────────────────────────────────────────┘
```

### 2.6 业务流程

#### 2.6.1 单样本故障诊断流程

```
用户输入14个传感器读数
        │
        ▼
前端数据验证（格式、范围）
        │
        ▼
发送POST /api/diagnose/single请求
        │
        ▼
后端接收数据 → StandardScaler标准化
        │
        ▼
RandomForestClassifier预测
        │
        ▼
返回故障类型 + 各类别概率
        │
        ▼
前端展示结果 → 自动保存到历史记录
```

#### 2.6.2 批量数据导入与诊断流程

```
用户选择CSV文件
        │
        ▼
前端Web Worker分块解析CSV
        │
        ▼
显示数据预览（前20行）
        │
        ▼
用户确认导入 → 分块发送到后端（每500条）
        │
        ▼
后端数据验证 → 存入SQLite数据库
        │
        ▼
用户选择批量诊断 → 发送全部数据
        │
        ▼
后端批量标准化 → 批量预测
        │
        ▼
返回所有诊断结果 → 前端展示
```

#### 2.6.3 运维决策流程

```
输入：故障类型 + RUL值 + 设备编号
        │
        ▼
风险评估矩阵计算
        │
        ├─ 故障类型=正常 且 RUL>100 → 低风险
        ├─ 故障类型=轻微 或 50<RUL≤100 → 中风险
        ├─ 故障类型=严重 或 20<RUL≤50 → 高风险
        └─ 故障类型=致命 或 RUL≤20 → 紧急
        │
        ▼
生成维护建议 + 优先级排序
        │
        ▼
返回决策结果 → 前端展示
```

### 2.7 假设与依赖

**假设条件：**

1. 用户提供的CSV数据格式符合系统要求（包含engine_id、cycle和s1-s21列）
2. 传感器数据已经过初步清洗，不存在明显的异常值和缺失值
3. 预训练的模型文件（fault_classifier.pkl和rul_predictor.pkl）已正确放置在指定目录
4. 系统运行在本地环境，不需要考虑网络安全和数据加密等生产环境问题
5. 用户具备基本的计算机操作能力，能够按照说明启动系统

**依赖条件：**

1. 依赖NASA发布的C-MAPSS涡扇发动机退化数据集进行模型训练
2. 依赖scikit-learn库提供的随机森林算法实现故障分类和RUL预测
3. 依赖FastAPI框架提供后端API服务
4. 依赖Vue3和Element Plus提供前端界面
5. 依赖SQLite提供数据存储

---

## 3. 具体需求

### 3.1 功能需求

#### 3.1.1 数据导入模块（FR-DI）

| 需求编号 | 需求描述 | 优先级 | 验收方法 |
|----------|----------|--------|----------|
| FR-DI-01 | 系统应支持CSV格式传感器数据文件上传，文件大小不超过50MB | 高 | 上传符合要求的CSV文件，验证上传成功 |
| FR-DI-02 | 上传过程中应显示实时进度条，支持取消上传操作 | 高 | 上传大文件，观察进度条更新，点击取消验证 |
| FR-DI-03 | 上传完成后应显示数据预览（前20行），包含列名和数据类型 | 高 | 上传文件后检查预览区域内容 |
| FR-DI-04 | 系统应支持13096条及以上数据的批量导入，导入时间不超过10秒 | 高 | 上传test.csv（13096条），计时验证 |
| FR-DI-05 | 万级数据导入过程中页面不应卡顿、不白屏，可正常操作其他功能 | 高 | 导入过程中切换页面、点击按钮验证 |
| FR-DI-06 | 系统应支持引擎编号远程搜索下拉框，不一次性加载全部选项 | 中 | 输入引擎编号关键字，验证搜索结果 |
| FR-DI-07 | 系统应对导入数据进行格式校验：检查必填列是否存在、数据类型是否正确 | 中 | 上传缺少必填列的文件，验证错误提示 |
| FR-DI-08 | 导入失败时应显示具体错误信息（行号、错误原因），支持修正后重新上传 | 中 | 上传包含错误数据的文件，验证错误提示 |

#### 3.1.2 故障诊断模块（FR-FD）

| 需求编号 | 需求描述 | 优先级 | 验收方法 |
|----------|----------|--------|----------|
| FR-FD-01 | 系统应支持单样本手动输入：提供14个传感器读数的表单输入，带输入验证 | 高 | 手动输入测试样本，验证诊断结果 |
| FR-FD-02 | 系统应支持批量数据导入诊断：从已导入的数据中选择样本进行批量诊断 | 高 | 选择已导入数据，执行批量诊断 |
| FR-FD-03 | 系统应输出故障类型分类结果（正常、风扇故障、压气机故障、涡轮故障等） | 高 | 输入已知故障类型的样本，验证输出 |
| FR-FD-04 | 系统应输出各类别概率（置信度），显示最高概率类别及其概率值 | 高 | 检查诊断结果页面是否显示概率值 |
| FR-FD-05 | 单样本诊断响应时间应不超过1秒 | 高 | 连续测试10次，取平均响应时间 |
| FR-FD-06 | 13096条批量诊断响应时间应不超过10秒 | 高 | 对13096条数据执行批量诊断，计时 |
| FR-FD-07 | 诊断结果应自动保存到历史记录，可随时查询 | 中 | 执行诊断后查看历史记录 |
| FR-FD-08 | 系统应支持诊断结果导出为CSV文件 | 低 | 点击导出按钮，验证CSV文件内容 |

#### 3.1.3 寿命预测模块（FR-RUL）

| 需求编号 | 需求描述 | 优先级 | 验收方法 |
|----------|----------|--------|----------|
| FR-RUL-01 | 系统应支持单样本RUL定量预测，输入14个传感器读数和运行周期 | 高 | 输入测试样本，验证RUL预测值 |
| FR-RUL-02 | 系统应输出RUL预测值（0-125个运行周期） | 高 | 检查预测结果是否在合理范围内 |
| FR-RUL-03 | RUL预测值应按阈值划分为四个等级：健康（>100）、关注（50-100）、警告（20-50）、危险（<20） | 高 | 输入不同RUL范围的样本，验证等级 |
| FR-RUL-04 | 不同寿命等级应用不同颜色标识（绿色/黄色/橙色/红色） | 中 | 检查页面颜色标识是否正确 |
| FR-RUL-05 | 系统应支持RUL历史趋势曲线可视化（ECharts折线图） | 中 | 查看设备历史预测记录，验证趋势图 |
| FR-RUL-06 | 单样本RUL预测响应时间应不超过1秒 | 高 | 连续测试10次，取平均响应时间 |
| FR-RUL-07 | 预测结果应自动保存到历史记录 | 中 | 执行预测后查看历史记录 |

#### 3.1.4 运维决策模块（FR-MD）

| 需求编号 | 需求描述 | 优先级 | 验收方法 |
|----------|----------|--------|----------|
| FR-MD-01 | 系统应基于故障类型和RUL的二维风险评估矩阵，输出四级风险等级 | 高 | 输入不同组合，验证风险等级 |
| FR-MD-02 | 风险等级应定义为：低风险（绿色）、中风险（黄色）、高风险（橙色）、紧急（红色） | 高 | 检查风险等级定义和颜色 |
| FR-MD-03 | 系统应根据风险等级生成配套维护建议（继续运行/增加监测/尽快维护/立即停机） | 高 | 输入不同风险等级，验证建议内容 |
| FR-MD-04 | 系统应显示决策依据：故障类型、RUL值、风险等级、建议措施 | 中 | 检查决策结果页面是否显示依据 |
| FR-MD-05 | 系统应支持多设备维护优先级排序（按风险等级和RUL值排序） | 中 | 输入多设备数据，验证排序结果 |
| FR-MD-06 | 运维决策响应时间应不超过1秒 | 高 | 连续测试10次，取平均响应时间 |

#### 3.1.5 历史记录模块（FR-HR）

| 需求编号 | 需求描述 | 优先级 | 验收方法 |
|----------|----------|--------|----------|
| FR-HR-01 | 系统应支持诊断记录和预测记录的列表展示，分页加载 | 高 | 查看历史记录页面，验证列表展示 |
| FR-HR-02 | 系统应支持按时间范围筛选记录 | 中 | 选择时间范围，验证筛选结果 |
| FR-HR-03 | 系统应支持按设备编号/引擎编号筛选记录 | 中 | 输入设备编号，验证筛选结果 |
| FR-HR-04 | 系统应支持按故障类型/风险等级筛选记录 | 中 | 选择故障类型，验证筛选结果 |
| FR-HR-05 | 点击记录应可查看完整详情（输入参数、输出结果、时间戳） | 高 | 点击记录，验证详情展示 |
| FR-HR-06 | 系统应支持记录数据导出为CSV文件 | 低 | 点击导出按钮，验证CSV文件 |
| FR-HR-07 | 系统应支持单条记录删除和批量删除 | 低 | 选择记录，执行删除操作 |

#### 3.1.6 系统设置模块（FR-SS）

| 需求编号 | 需求描述 | 优先级 | 验收方法 |
|----------|----------|--------|----------|
| FR-SS-01 | 系统应支持后端API地址配置（默认http://localhost:8000） | 中 | 修改API地址，保存后验证请求目标 |
| FR-SS-02 | 系统应支持前端主题切换（浅色/深色） | 低 | 切换主题，验证界面变化 |
| FR-SS-03 | 系统应显示系统信息（版本号、技术栈、模型文件状态） | 低 | 查看系统信息页面 |
| FR-SS-04 | 系统应支持一键启动后端服务（start.bat脚本） | 中 | 双击start.bat，验证后端启动 |

### 3.2 功能需求追踪矩阵

| 需求编号 | 设计模块 | 测试用例 | 优先级 |
|----------|----------|----------|--------|
| FR-DI-01 ~ FR-DI-08 | 数据导入页面 + 后端上传接口 | TC-DI-01 ~ TC-DI-08 | 高/中 |
| FR-FD-01 ~ FR-FD-08 | 故障诊断页面 + 后端诊断接口 | TC-FD-01 ~ TC-FD-08 | 高/中 |
| FR-RUL-01 ~ FR-RUL-07 | 寿命预测页面 + 后端预测接口 | TC-RUL-01 ~ TC-RUL-07 | 高/中 |
| FR-MD-01 ~ FR-MD-06 | 运维决策页面 + 后端决策接口 | TC-MD-01 ~ TC-MD-06 | 高/中 |
| FR-HR-01 ~ FR-HR-07 | 历史记录页面 + 后端查询接口 | TC-HR-01 ~ TC-HR-07 | 高/中/低 |
| FR-SS-01 ~ FR-SS-04 | 系统设置页面 + 启动脚本 | TC-SS-01 ~ TC-SS-04 | 中/低 |

---

## 4. 外部接口需求

### 4.1 用户界面

#### 4.1.1 界面风格

- 整体风格：简洁、专业、科技感
- 主色调：深蓝色（#002FA7），代表专业和可靠
- 辅助色：暖白色（#F5F3EE）背景，提升可读性
- 强调色：绿色（正常/低风险）、黄色（关注/中风险）、橙色（警告/高风险）、红色（危险/紧急）
- 字体：标题使用黑体，正文使用宋体
- 布局：顶部导航栏 + 左侧菜单 + 右侧内容区的经典后台管理布局

#### 4.1.2 页面清单

| 页面名称 | 路由路径 | 功能描述 |
|----------|----------|----------|
| 首页/仪表盘 | / | 系统概览、设备状态统计、最近诊断记录 |
| 数据导入 | /data-import | CSV文件上传、数据预览、批量导入 |
| 故障诊断 | /fault-diagnosis | 单样本诊断、批量诊断、结果展示 |
| 寿命预测 | /rul-prediction | RUL预测、寿命等级、趋势图 |
| 运维决策 | /maintenance-decision | 风险评估、维护建议、优先级排序 |
| 历史记录 | /history | 记录查询、筛选、详情、导出 |
| 系统设置 | /settings | API配置、主题切换、系统信息 |

### 4.2 硬件接口

本系统为纯软件系统，不直接与硬件设备交互。传感器数据通过CSV文件导入系统，不支持实时硬件数据采集。

### 4.3 软件接口

#### 4.3.1 后端API接口

系统后端提供以下RESTful API接口，所有接口统一使用JSON格式进行数据交换。

**基础URL：** `http://localhost:8000/api`

| 接口路径 | 方法 | 功能 | 认证 |
|----------|------|------|------|
| /data/upload | POST | CSV数据文件上传 | 无 |
| /data/list | GET | 获取已导入数据列表 | 无 |
| /diagnose/single | POST | 单样本故障诊断 | 无 |
| /diagnose/batch | POST | 批量故障诊断 | 无 |
| /predict/rul | POST | 剩余使用寿命预测 | 无 |
| /maintenance/decision | POST | 运维决策建议生成 | 无 |
| /history | GET | 历史记录查询 | 无 |

#### 4.3.2 接口详细规格

**（1）单样本故障诊断接口**

- **请求URL：** `POST /api/diagnose/single`
- **请求头：** `Content-Type: application/json`
- **请求体：**

```json
{
  "features": [641.82, 1589.70, 1400.60, 554.36, 2388.06, 9046.19, 47.47, 521.66, 2388.02, 8138.62, 8.4195, 0.03, 392, 2046]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| features | array[float] | 是 | 14个传感器读数，顺序固定 |

- **响应体：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "fault_type": "正常",
    "confidence": 0.92,
    "probabilities": {
      "正常": 0.92,
      "风扇故障": 0.03,
      "压气机故障": 0.03,
      "涡轮故障": 0.02
    }
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| fault_type | string | 故障类型分类结果 |
| confidence | float | 最高类别概率（置信度） |
| probabilities | object | 各类别概率字典 |

**（2）RUL预测接口**

- **请求URL：** `POST /api/predict/rul`
- **请求体：**

```json
{
  "features": [641.82, 1589.70, 1400.60, 554.36, 2388.06, 9046.19, 47.47, 521.66, 2388.02, 8138.62, 8.4195, 0.03, 392, 2046],
  "engine_id": 1,
  "cycle": 50
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| features | array[float] | 是 | 14个传感器读数 |
| engine_id | int | 是 | 设备/发动机编号 |
| cycle | int | 是 | 当前运行周期数 |

- **响应体：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "rul_value": 85,
    "rul_level": "关注",
    "confidence": 0.88
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| rul_value | int | RUL预测值（运行周期数） |
| rul_level | string | 寿命等级（健康/关注/警告/危险） |
| confidence | float | 预测置信度 |

**（3）运维决策接口**

- **请求URL：** `POST /api/maintenance/decision`
- **请求体：**

```json
{
  "fault_type": "压气机故障",
  "rul_value": 30,
  "engine_id": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| fault_type | string | 是 | 故障类型 |
| rul_value | int | 是 | RUL预测值 |
| engine_id | int | 是 | 设备编号 |

- **响应体：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "risk_level": "高风险",
    "suggestion": "尽快安排维护（1-2周内），降低设备负载，增加监测频率",
    "priority": 2,
    "reason": "检测到压气机故障，剩余使用寿命30个周期，处于警告区间"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| risk_level | string | 风险等级（低风险/中风险/高风险/紧急） |
| suggestion | string | 维护建议 |
| priority | int | 优先级（1-4，数字越小优先级越高） |
| reason | string | 决策依据说明 |

**（4）通用响应格式**

所有接口统一使用以下响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

错误响应：

```json
{
  "code": 400,
  "message": "参数错误：features必须是14维数组",
  "data": null
}
```

| 错误码 | 含义 |
|--------|------|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 4.4 通信接口

- 前后端通信协议：HTTP/1.1
- 数据交换格式：JSON（UTF-8编码）
- 文件上传格式：multipart/form-data
- 跨域配置：CORS允许所有来源（allow_origins=["*"]）
- 字符编码：UTF-8

---

## 5. 数据需求

### 5.1 输入数据格式

系统接受CSV格式的传感器数据，文件编码支持UTF-8和GBK自动识别。

#### 5.1.1 必填列

| 列名 | 数据类型 | 说明 | 示例值 |
|------|----------|------|--------|
| engine_id | 整数 | 发动机/设备编号 | 1 |
| cycle | 整数 | 运行周期数 | 50 |
| s1 ~ s21 | 浮点数 | 21个传感器测量值 | 641.82 |

#### 5.1.2 有效特征列

系统从21个传感器中自动筛选以下14个有效特征用于模型推理：

s2, s3, s4, s7, s8, s9, s10, s11, s12, s13, s14, s15, s17, s21

#### 5.1.3 数据示例

```csv
engine_id,cycle,s1,s2,s3,s4,s5,s6,s7,s8,s9,s10,s11,s12,s13,s14,s15,s16,s17,s18,s19,s20,s21
1,1,0.0010,0.0000,100.0,518.67,641.82,1589.70,1400.60,14.62,21.61,554.36,2388.06,9046.19,1.30,47.47,521.66,2388.02,8138.62,8.4195,0.03,392,2046
1,2,0.0015,0.0000,100.0,518.67,642.15,1591.82,1403.14,14.62,21.61,553.75,2388.04,9044.07,1.30,47.49,521.38,2388.07,8131.49,8.4318,0.03,392,2036
```

### 5.2 数据库设计

系统使用SQLite轻量级数据库，数据库文件为 `backend/data/equipment.db`。

#### 5.2.1 设备信息表（equipment）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| engine_id | INTEGER | UNIQUE, NOT NULL | 设备编号 |
| model | TEXT | | 设备型号 |
| install_date | DATE | | 安装日期 |
| status | TEXT | DEFAULT '正常' | 设备状态（正常/维护中/故障） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 5.2.2 传感器数据表（sensor_data）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| engine_id | INTEGER | NOT NULL, INDEX | 设备编号，外键 |
| cycle | INTEGER | NOT NULL | 运行周期 |
| s1 ~ s21 | REAL | | 21个传感器读数 |
| timestamp | DATETIME | | 数据采集时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 导入时间 |

#### 5.2.3 故障记录表（fault_record）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| engine_id | INTEGER | NOT NULL, INDEX | 设备编号 |
| fault_type | TEXT | NOT NULL | 故障类型 |
| confidence | REAL | | 置信度（0-1） |
| severity | TEXT | | 严重程度（低/中/高/紧急） |
| detected_at | DATETIME | | 检测时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 记录时间 |

#### 5.2.4 预测结果表（prediction）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| engine_id | INTEGER | NOT NULL, INDEX | 设备编号 |
| rul_value | INTEGER | NOT NULL | RUL预测值（周期数） |
| rul_level | TEXT | | 寿命等级（健康/关注/警告/危险） |
| confidence | REAL | | 预测置信度 |
| predicted_at | DATETIME | | 预测时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 记录时间 |

#### 5.2.5 维护记录表（maintenance）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| engine_id | INTEGER | NOT NULL, INDEX | 设备编号 |
| maintenance_type | TEXT | | 维护类型（例行/故障/预防性） |
| description | TEXT | | 维护描述 |
| risk_level | TEXT | | 维护时风险等级 |
| maintained_at | DATETIME | | 维护时间 |
| maintained_by | TEXT | | 维护人员 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 记录时间 |

### 5.3 模型文件

系统依赖两个预训练的机器学习模型文件，使用Python pickle格式序列化。

| 文件名 | 大小 | 存储路径 | 内容结构 | 用途 |
|--------|------|----------|----------|------|
| fault_classifier.pkl | 7.27MB | backend/models/ | {"model": RandomForestClassifier, "scaler": StandardScaler} | 故障类型分类 |
| rul_predictor.pkl | 88.09MB | backend/models/ | {"model": RandomForestRegressor, "scaler": StandardScaler} | RUL定量预测 |

**模型加载方式：** 后端服务启动时通过FastAPI的startup事件一次性加载两个模型到内存，避免每次请求重复加载。

---

## 6. 非功能需求

### 6.1 性能需求

| 指标 | 要求 | 测试条件 | 优先级 |
|------|------|----------|--------|
| 单样本故障诊断响应时间 | ≤ 1秒 | 普通笔记本，1条样本 | 高 |
| 单样本RUL预测响应时间 | ≤ 1秒 | 普通笔记本，1条样本 | 高 |
| 运维决策响应时间 | ≤ 1秒 | 普通笔记本，1次决策 | 高 |
| 万级数据导入时间 | ≤ 10秒 | 13096条CSV数据 | 高 |
| 万级批量诊断时间 | ≤ 10秒 | 13096条样本 | 高 |
| 前端页面加载时间 | ≤ 3秒 | 首页首次加载 | 中 |
| 系统并发支持 | ≥ 5个并发用户 | 同时进行诊断操作 | 中 |
| 内存占用 | ≤ 2GB | 系统稳定运行时 | 中 |
| CPU占用 | ≤ 80% | 批量诊断时 | 低 |

### 6.2 可用性需求

- 系统界面应简洁直观，运维工程师无需培训即可完成基本操作
- 所有操作应提供明确的反馈（加载状态、成功提示、错误提示）
- 错误提示信息应清晰易懂，包含错误原因和建议操作
- 系统应支持一键启动（start.bat），无需复杂的环境配置
- 系统应在普通笔记本电脑（8GB内存，i5处理器）上稳定运行
- 页面响应式设计，支持1366×768及以上分辨率

### 6.3 可靠性需求

- 系统全年可用率应不低于95%
- 数据导入失败时不应影响已有数据，支持断点续传
- 后端服务异常时前端应显示友好提示，不白屏不崩溃
- 数据库应支持自动备份（每日一次），数据丢失不超过24小时
- 模型文件加载失败时应给出明确错误提示，不静默失败
- 系统应具备异常恢复能力，重启后数据不丢失

### 6.4 可维护性需求

- 代码结构应清晰，模块化设计，前后端分离
- 关键代码应添加注释，复杂算法应提供文档说明
- 模型文件应可替换，无需修改代码即可升级模型
- API接口应遵循RESTful规范，提供接口文档
- 数据库表结构设计应合理，支持后续扩展
- 配置项应集中管理，避免硬编码

### 6.5 安全性需求

- 前后端通信使用HTTP协议（本地部署场景），生产环境应升级为HTTPS
- 输入数据应进行格式验证和类型检查，防止非法数据注入
- 数据库文件应设置访问权限，防止未授权访问
- 系统不应收集用户个人信息，所有数据本地存储
- API接口应添加基本的请求频率限制，防止滥用
- 文件上传应限制文件类型和大小，防止恶意文件上传

### 6.6 兼容性需求

- 前端应支持Chrome、Edge、Firefox等主流浏览器的最新两个版本
- 后端应支持Windows 10/11操作系统
- Python版本要求3.8及以上
- 应支持CSV文件的UTF-8和GBK编码自动识别
- 应支持不同分辨率的显示器（1366×768至1920×1080）

### 6.7 可移植性需求

- 系统应支持在不同Windows电脑上迁移运行
- 依赖库应通过requirements.txt明确指定版本
- 前端应支持npm run build打包为静态文件，可部署到任意Web服务器
- 数据库文件应可直接复制迁移

---

## 7. 验收标准

### 7.1 功能验收

| 验收项 | 验收标准 | 测试方法 | 通过条件 |
|--------|----------|----------|----------|
| 数据导入 | 支持CSV上传，13096条数据10秒内导入完成，页面不卡顿 | 上传test.csv，计时并观察页面响应 | 导入成功，时间≤10秒，页面可操作 |
| 单样本诊断 | 输入14个传感器值，1秒内返回故障类型和置信度 | 手动输入测试样本，验证结果 | 响应时间≤1秒，结果正确 |
| 批量诊断 | 13096条数据批量诊断，10秒内完成，返回所有结果 | 选择已导入数据，执行批量诊断 | 完成时间≤10秒，结果完整 |
| RUL预测 | 输入传感器值和周期，1秒内返回RUL值和等级 | 手动输入测试样本，验证预测结果 | 响应时间≤1秒，RUL值在0-125范围 |
| 运维决策 | 输入故障类型和RUL，返回风险等级和维护建议 | 组合不同输入，验证决策逻辑 | 风险等级正确，建议合理 |
| 历史记录 | 诊断/预测结果自动保存，可按条件筛选查询 | 执行多次诊断，验证记录和筛选 | 记录完整，筛选正确 |
| 系统设置 | API地址可配置，保存后生效 | 修改API地址，验证请求目标 | 配置保存成功，请求目标正确 |

### 7.2 性能验收

| 验收项 | 验收标准 | 测试方法 | 通过条件 |
|--------|----------|----------|----------|
| 单样本诊断响应 | ≤ 1秒 | 连续测试10次，取平均值 | 平均响应时间≤1秒 |
| 单样本RUL预测响应 | ≤ 1秒 | 连续测试10次，取平均值 | 平均响应时间≤1秒 |
| 万级数据导入 | ≤ 10秒 | 上传13096条test.csv，计时 | 导入时间≤10秒 |
| 万级批量诊断 | ≤ 10秒 | 对13096条数据执行批量诊断，计时 | 诊断时间≤10秒 |
| 页面加载 | ≤ 3秒 | 清空缓存后首次加载首页，计时 | 加载时间≤3秒 |
| 内存占用 | ≤ 2GB | 系统稳定运行时查看任务管理器 | 内存占用≤2GB |

### 7.3 可靠性验收

| 验收项 | 验收标准 | 测试方法 | 通过条件 |
|--------|----------|----------|----------|
| 异常输入处理 | 输入非法数据时显示友好提示，不崩溃 | 输入空值、错误类型、超范围值 | 提示信息清晰，系统不崩溃 |
| 后端异常处理 | 后端服务停止时前端显示提示，不白屏 | 停止后端服务，操作前端功能 | 显示友好提示，页面不白屏 |
| 大文件处理 | 上传超过50MB文件时提示文件过大 | 上传大文件，验证提示信息 | 提示文件过大，不上传 |
| 数据完整性 | 导入失败时不丢失已有数据 | 中途取消上传，验证已有数据 | 已有数据完整，不丢失 |

### 7.4 文档验收

| 验收项 | 验收标准 |
|--------|----------|
| README.md | 包含项目介绍、技术栈、安装步骤、使用说明、目录结构 |
| 需求规格说明书.md | 包含功能需求、非功能需求、数据需求、接口需求、验收标准 |
| 方案设计.md | 包含架构设计、模块设计、数据库设计、算法设计 |
| API接口文档 | 包含所有接口的请求参数、响应格式、示例代码 |
| 代码注释 | 关键函数和复杂逻辑添加注释，注释覆盖率≥30% |

---

## 8. 项目约束

### 8.1 技术约束

- 前端框架必须使用Vue 3 + Vite + Element Plus + ECharts
- 后端框架必须使用Python 3.8+ + FastAPI + Uvicorn
- 机器学习算法必须使用scikit-learn的随机森林算法
- 数据库必须使用SQLite（轻量级，无需独立服务）
- 操作系统必须支持Windows 10/11
- 浏览器必须支持Chrome/Edge/Firefox最新两个版本

### 8.2 数据约束

- 输入数据必须为CSV格式，UTF-8或GBK编码
- 单文件大小不超过50MB
- 单次批量诊断不超过20000条样本
- 传感器数据必须包含engine_id和cycle列
- 14个有效特征列必须存在且为数值类型
- 模型训练数据必须使用NASA C-MAPSS数据集

### 8.3 性能约束

- 系统必须在普通笔记本电脑（8GB内存，i5处理器）上稳定运行
- 内存占用不超过2GB
- 单样本诊断响应时间不超过1秒
- 万级数据批量分析不超过10秒

### 8.4 部署约束

- 系统支持本地单机部署，无需云服务器
- 一键启动脚本（start.bat）自动启动后端服务
- 前端可通过npm run dev启动开发服务器，或npm run build打包为静态文件
- 模型文件必须与后端代码在同一目录结构中

### 8.5 进度约束

- 项目总周期为11周（2026年6月至2026年9月）
- 第9周完成系统开发和测试
- 第10周完成文档撰写
- 第11周进行答辩验收

---

## 9. 附录

### 附录A：风险评估

| 风险 | 影响 | 概率 | 风险等级 | 应对措施 |
|------|------|------|----------|----------|
| 模型准确率不达标 | 诊断结果不可靠 | 中 | 中 | 尝试多种算法，调整超参数，增加特征工程 |
| 万级数据导入卡顿 | 用户体验差 | 高 | 高 | Web Worker分块解析、分块传输、非响应式数据 |
| 模型文件过大加载慢 | 启动时间长 | 中 | 中 | 模型压缩、启动时预加载、懒加载 |
| 前后端联调问题 | 开发进度延迟 | 中 | 中 | 提前定义API规范，使用Mock数据并行开发 |
| 数据库性能瓶颈 | 查询响应慢 | 低 | 低 | 添加索引、分页查询、限制单次返回条数 |
| 浏览器兼容性问题 | 部分用户无法使用 | 低 | 低 | 限定支持浏览器版本，使用Polyfill |

### 附录B：项目里程碑

| 阶段 | 时间 | 交付物 | 负责人 |
|------|------|--------|--------|
| 需求分析 | 第1周 | 需求规格说明书、选题说明 | 罗志勇 |
| 方案设计 | 第2周 | 方案设计、系统架构图、数据库设计 | 罗志勇 |
| 数据准备 | 第3周 | C-MAPSS数据集、预处理脚本、模型文件 | 罗志勇 |
| 后端开发 | 第4-5周 | FastAPI后端代码、7个API接口 | 罗志勇 |
| 前端开发 | 第6-7周 | Vue3前端代码、6个功能页面 | 罗志勇 |
| 算法集成 | 第8周 | 故障分类、RUL预测、运维决策模块 | 罗志勇 |
| 测试调试 | 第9周 | 测试报告、Bug修复 | 罗志勇 |
| 文档撰写 | 第10周 | 课程设计报告、答辩PPT | 罗志勇 |
| 答辩验收 | 第11周 | 系统演示、答辩汇报 | 罗志勇 |

### 附录C：变更记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| V1.0 | 2026-09-03 | 初始版本，完成需求规格说明书编制 | 罗志勇 |

---

